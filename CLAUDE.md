# SMART Hub · APM Terminals Mobile

Este archivo se lee automáticamente al abrir este repositorio con Claude Code. Contiene el contexto completo del proyecto para que puedas continuar el trabajo sin perder lo ya avanzado.

## Resumen del proyecto

- **Nombre del portal:** **SMART Hub** (APM Terminals Mobile · Operational Intelligence Hub).
- **Propósito:** Portal centralizado estático (*client-side hub*) para alojar micro-herramientas operativas, simuladores y generadores de reportes para las operaciones de APM Terminals Mobile (Alabama), organizadas por **área operativa**.
- **Arquitectura de origen:** Inspirado en la estructura base de *BrainPort*; el contenido y las herramientas de ejemplo que traía esa base ya se eliminaron — el hub arranca vacío y cada área se llena a medida que se construye una herramienta real para APM Terminals Mobile.
- **Dueño:** Alex Pinzon (Operations / Strategy Team).

## Idioma: inglés siempre

**Todo el contenido visible de la plataforma (`index.html` y cada herramienta en `tools/`) debe estar en inglés americano profesional, con terminología de puertos y terminales de contenedores** (vessel, berth, yard, gate, rail, stowage, gate-in/gate-out, prime mover, etc.). No usar español en texto de UI, aunque el usuario escriba las instrucciones en español — traducir el contenido, no copiarlo literal. Comentarios de código y este `CLAUDE.md` sí pueden quedar en español (documentación interna para trabajar con Claude Code).

## Estado actual (confirmado y funcionando)

- **Repositorio:** `Al3xPinz0n/smarthub` en GitHub — **público** (necesario para GitHub Pages gratis). Renombrado desde `Company-Projects` (sep. 2026); el remoto local ya apunta a la nueva URL.
- **Sitio publicado:** `https://al3xpinz0n.github.io/smarthub/`.
- **Despliegue:** GitHub Pages desde `Settings > Pages`, origen `Deploy from a branch`, rama `main`, directorio `/(root)`.
- **Repo local:** carpeta `C:\Users\ADP037\Documents\GitHub\Company-Projects` (el nombre de la carpeta local quedó igual; solo cambió el repo remoto) — GitHub Desktop ya autenticado con la cuenta del usuario.
- **Flujo de publicación:** cualquier cambio en `main` se refleja automáticamente en el sitio (1-2 minutos) al hacer push — el usuario usa GitHub Desktop (Commit → Push) para esto, sin línea de comandos.
- **El hub arrancó vacío y se va llenando por herramienta real.** `TOOLS` en `index.html` ya no está vacío: **Strategy** tiene tres herramientas (`Yard Strategy`, `Export & Empty Analysis` y `SQDC Operational Review`) y **Vessel** tiene cuatro (`Vessel Move Reconciliation`, `Inbound BAPLIE Correction`, `Quay Commander` y `Breakbulk Quote Desk`), ver más abajo. Yard, Gate, Rail y VAS siguen mostrando su estado vacío ("No tools have been added...") hasta que se agregue su primera herramienta real.

## Diseño y marca

El hub sigue el skill `apm-terminals-brand` (colores, tipografía, logo e íconos oficiales de APM Terminals). **Revisar ese skill antes de tocar cualquier estilo o de construir una herramienta nueva.**

- **Tipografía:** Maersk Text (Light/Regular/Bold), cargada vía `@font-face` desde `assets/fonts/*.ttf`. Fallback: Arial.
- **Colores:** paleta oficial APM en `:root` de `index.html` — Orange `#FF6441`, Dark Gray `#3C3C46`, Aqua Green `#0A6E82`, Maersk Blue `#42B0D5`, Soft Yellow `#F0BE78`, grises `#5A6E7D` / `#AAB4C3` / `#E6EBF2`.
- **Logo:** SVG oficial embebido directamente en el `<header>` de `index.html`, a **236px de ancho** (2x el tamaño original — pedido explícito del usuario). No recrear, recolorear ni reducir el logo por debajo de esto sin que el usuario lo pida.
- **Íconos de área:** pictogramas oficiales de la familia `pictograms-orange.svg` del skill (tono naranja único, sin recoloreo), embebidos como sprite `<symbol>` al inicio de `index.html`. En la grilla de áreas se muestran a **85px** dentro de un contenedor de **150px** (2.5x el tamaño original — pedido explícito del usuario). Reutilizar ese mismo sprite para íconos nuevos en vez de emojis o íconos genéricos.
- **Cada herramienta debe tener, en su header: un botón "← SMART Hub" y un toggle de día/noche** (sep. 2026, pedido explícito del usuario, aplicado a las 5 herramientas existentes). Patrón estándar (copiar tal cual en herramientas nuevas):
  - Botón de vuelta: `<a href="../../../index.html">← SMART Hub</a>` (esa ruta relativa asume la profundidad `tools/<área>/<nombre>/index.html`; ajustar si la herramienta vive más profundo).
  - Toggle de tema: botón con dos `<svg>` (sol/luna) alternados vía CSS (`.icon-moon{display:none} html[data-theme="dark"] .icon-sun{display:none} html[data-theme="dark"] .icon-moon{display:block}`), que hace `document.documentElement.setAttribute('data-theme', 'dark'|'light')` y lo guarda en `localStorage` con una key **propia de cada herramienta** (no compartida) — p. ej. `yardstrategy-theme`, `exportempty-theme`, `vesselmove-theme`, `edi-tool-theme` (BAPLIE), `quaycommander-theme`. Un script mínimo en el `<head>`, antes de cualquier otro `<script>`, aplica el tema guardado (o `prefers-color-scheme`) antes del primer paint para evitar parpadeo.
  - El modo oscuro en sí se logra agregando un bloque `html[data-theme="dark"]{ --var: valor-oscuro; ... }` que sobreescribe las variables CSS que la herramienta ya usa en su `:root` (fondo/tarjeta/borde/texto) — no hace falta tocar cada regla individual si la herramienta ya está construida sobre variables CSS. Ver `tools/vessel/quay-commander/index.html` como referencia del patrón completo (fue la primera en implementarlo) y `tools/vessel/baplie-correction/index.html` (implementación independiente, ligeramente distinta pero equivalente).
  - Excepción conocida: `vessel-move-reconciliation` — el modo oscuro cubre el header y el fondo general, pero la zona de drag-and-drop de archivos (`.drop`) se quedó con fondo claro fijo; no se corrigió porque no formaba parte de lo pedido (solo agregar los botones), queda como ajuste visual menor pendiente si se retoma esa herramienta.

## Estructura de navegación: áreas operativas

El hub usa 6 tarjetas de **área operativa** en la página de inicio (no pestañas, no el término "gaveta" — ese fue solo un concepto usado para explicar la idea durante el diseño, nunca debe aparecer en el producto ni en el código). Cada desarrollo nuevo se agrega al área que le corresponda (arreglo `TOOLS` dentro de `index.html`, cada entrada con su `area` matching una de estas keys):

| Área     | Alcance |
|----------|---------|
| Strategy | Operational planning, performance targets, and ABS reporting for the Terminal |
| Vessel   | Vessels at berth, stowage planning, crane productivity |
| Yard     | Yard status, prime mover flow, operator performance |
| Gate     | Appointments, gate-in/gate-out processing, truck flow |
| Rail     | Rail operations scheduling, railcar movements |
| VAS      | Value-added services (stuffing, stripping, etc.) |

**Strategy** ya tiene sus dos primeras herramientas reales (construidas fuera de este repo y luego incorporadas):

| Herramienta | Carpeta | Qué hace |
|---|---|---|
| **Yard Strategy** | `tools/strategy/yard-strategy/` | Mapa interactivo de bloques del patio para definir y ajustar la estrategia de yard — clasificar bloques, asignar servicios/líneas y revisar capacidad por clase. Tiene su propio candado interno adicional para la pestaña "Configuration" (contraseña embebida en el JS del archivo, `StrategyMobile` — es de ese archivo, no del gate de SMART Hub; no lo confundas con `AREA_HASHES`). ⚠️ **Excepción a "100% client-side, cero backend":** esta es la única herramienta del hub con estado compartido real — usa **Firebase Firestore** (proyecto `apmt-mobile-smarthub`, cuenta del usuario, plan gratuito Spark) en vez de `localStorage`, para que un cambio guardado por cualquier persona desde Configuration se vea al instante en todas las pantallas que tengan la app abierta (verificado en vivo con dos pestañas). El objeto `FIREBASE_CONFIG` embebido en el JS es información pública (así es como Firebase espera que se distribuya su config web; la protección real está en las Firestore Security Rules, no en ocultar esas claves). Las Security Rules del proyecto están en modo abierto (`allow read, write: if true` para `yardStrategy/*`) — mismo espíritu que el resto de SMART Hub: un candado liviano (`StrategyMobile`), no autenticación real. Nunca subir datos operativos sensibles a este documento. |
| **Export & Empty Analysis** | `tools/strategy/export-empty-analysis/` | El usuario sube dos Excel (lista de movimientos de carga + detalle de visitas de buque) y la herramienta calcula TEUs de exportación por POD/servicio, perfil de peso y TEUs vacíos por línea, usando SheetJS 100% en el navegador. |
| **SQDC Operational Review** | *No vive en este repo* — es un **Claude Artifact** (`https://claude.ai/artifact/RvjdKHYqfj7DGKNXY2VEZS`), enlazado desde `TOOLS` en `index.html` con `external:true` (abre en pestaña nueva, `target="_blank"`). Revisión mensual de Safety/Quality/Delivery/Cost por buque: captura de datos SQDC, registro de acciones de la reunión (abiertas/cerradas), próximos buques/retos/oportunidades, log consolidado de acciones, biblioteca mensual SQDC, y generación de la presentación ejecutiva. Usa las capacidades `db` (guardado compartido de reportes/acciones) y `downloads` (exportar la presentación) del runtime de Artifacts — **por eso no se copió el HTML al repo**: fuera del entorno de Claude (`window.claude` no existe en una página servida por GitHub Pages), el guardado simplemente no funcionaría. ⚠️ Igual que con Yard Strategy/Firebase, la capacidad `db` es **organization-internal** — solo miembros con sesión iniciada en la organización de claude.ai del dueño pueden leer/escribir los datos compartidos; el equipo de APM Terminals Mobile no tiene esas cuentas (confirmado por el usuario). El usuario ya cambió el "Share" del artifact a **"Anyone with the link"** (estaba en "private" por defecto) — el resto del equipo puede abrirlo desde el link del hub; el guardado compartido (`db`) en sí sigue limitado a quien tenga sesión en la organización de claude.ai del dueño. |

**Vessel** tiene cuatro herramientas:

| Herramienta | Carpeta | Qué hace |
|---|---|---|
| **Vessel Move Reconciliation** | `tools/vessel/vessel-move-reconciliation/` | El usuario sube la lista de movimientos de contenedores de una escala de buque (xlsx/xls/csv) y la herramienta concilia discharge, load y restow por categoría de contenedor (Dry, Reefer, Tank, Flat Rack, Pallet Wide) y tamaño, desglosado por naviera. Usa SheetJS, 100% en el navegador. ⚠️ Esta herramienta usa la tipografía Google Fonts "Plus Jakarta Sans" y valores de color aproximados en vez de Maersk Text / la paleta exacta del skill `apm-terminals-brand` — se incorporó tal cual porque ya funcionaba y el usuario solo pidió agregarla; si en algún momento se quiere alinear 100% a la marca, es un ajuste pendiente. |
| **Inbound BAPLIE Correction** | `tools/vessel/baplie-correction/` | El usuario carga un EDI/BAPLIE de entrada; la herramienta corre validaciones obligatorias (bloquea si hay `+PHA+` o `TBD_` sin corregir), identifica automáticamente la naviera (SSL) por posición estructural en `UNB+`/`TDT+` (con banner de conflicto o selección manual si no hay match único), aplica **4 reglas generales que corren para las 8 navieras** (`LOC+9`: `XXVUT`→`VNVUT`, `XXCMT`→`VNCMT`, `NLDDN`→`NLRTM`; `NAD+` que termina en `:172:ZZZ`→`:172:20`) más las reglas de corrección propias de esa naviera (de las 8 soportadas: MAE, CMA, ZIM, COS, OCL, ONE, EGL, MSC), y genera el archivo corregido listo para el TOS. 100% en el navegador, sin backend. Actualizada (sep. 2026) para agregar esas 4 reglas generales — antes solo corrían las reglas específicas de cada SSL; verificado con casos sintéticos que las reglas generales y las de SSL se aplican juntas correctamente (incl. MSC, que antes no tocaba el archivo y ahora si las reglas generales aplican reporta el cambio en vez de "no change needed"). Documentación completa del spec: `Inbound_BAPLIE_Correction_Tool_Documentation.md` (en `C:\Users\ADP037\Downloads`, no está en este repo). Ya sigue la marca al 100% (Maersk Text embebido, paleta exacta, modo día/noche). |
| **Quay Commander** | `tools/vessel/quay-commander/` | Optimizador de split de grúas con diagrama de secuenciación tipo Quay Commander (N4). El usuario sube BAPLIE + MOVINS (drag-and-drop o click, con botón para **borrar los archivos cargados y empezar de nuevo**) y opcionalmente un listado de restows; elige fecha + **hora de arranque restringida a las horas canónicas de "Inicios de Operaciones"** (07:00, 08:00, 10:00, 13:00, 15:00, 19:00, 00:00); costado de atraque; **muelle Sur/Norte define el orden de prioridad de selección de grúas** (ya no es solo informativo, ver "Prioridad de grúas por muelle" más abajo — pasada undécima); grúa(s) fuera de servicio elegidas en un **dropdown** cuya primera opción es "All cranes available"; y un selector de **indicador prioritario** (MHPM & UGT, o PHS % & PMPH) que determina cuál par de KPIs pesa más al elegir el escenario recomendado (los otros KPIs quedan como criterio de desempate). No hay inputs de tarifa manual — el ingreso se calcula con la tarifa real de `APMT Cobra` (imágenes embebidas en esa pestaña del xlsx de costos, extraídas vía COM/Excel Shapes): lift rate por contenedor lleno/vacío × 20'-40' o 45', recargo OOG, recargo por grúas adicionales (+15% con 3 grúas, +20% con 4+, ya que la tarifa base incluye 2 grúas), tarifa de restow, e ingreso por Standby/Guarantee sobre el Total UGT. La herramienta simula el trabajo bahía-por-bahía (fusión impar-par-impar de bahías, detección de twin lift por peso/row/tier, regla de arranque de dual cycle en bodega —OOG primero, 1 row 100%, row siguiente a 75%, luego 42 moves/hr combinado—, contenedores OOG a 3.7 min/move vs 1.85 min/move regular, y las pausas de **Meal Break — 00:00-01:00, 05:50-07:00, 11:50-13:00 y 17:50-19:00**, sin operaciones durante esas 4 ventanas) para asignar bahías a grúas específicas (contiguo, en orden numérico sur→norte entre las grúas disponibles) y produce 3 escenarios (normalmente 1-3 grúas, hasta 4 si hace falta) con un **diagrama Gantt SVG por escenario** con columnas de bahía más angostas, símbolos ▲/▼ combinados (▼▲) cuando un bloque tiene descarga y carga a la vez, bandas rayadas en los Meal Breaks, y tooltip con tiempo/moves/twin lifts/dual cycle/OOG/breaks de esa grúa. **UGT se calcula por grúa individualmente**: para el último movimiento de CADA grúa se identifica el turno (de "Datos del Turno") que mejor cubre esa duración, y UGT = garantía de ese turno − tiempo trabajado. Los KPIs de la Sección 3: Total Moves en vez de "Cranes used"; **Cost/Revenue/Profit vs. recomendado son solo % (sin $), con semáforo por signo** (rojo si el costo es mayor, verde si el ingreso o la ganancia son mayores, no por umbral fijo); CI calculado con la fórmula real del Excel; MHPM con target ≤0.92. El reporte PDF es landscape estilo presentación (portada con recuadro destacado, pie de página con numeración de diapositiva en todas las páginas), con una diapositiva por escenario con su Gantt (SVG→PNG vía canvas), conclusión y notas de metodología actualizadas. **Fuentes:** `resumen_proyecto_quay_commander_mobile.md`, `Criterio de costos para las operaciones de buque.xlsx` (incl. imágenes de `APMT Cobra`), `Berths & Cranes.png` / `QC.png` (en `C:\Users\ADP037\Downloads`, no están en este repo). Sigue la excepción explícita del usuario a "nunca hardcodear datos reales" para las tarifas/costos laborales (ver memoria `quay-commander-crane-cost-criteria`). **Simplificaciones aún vigentes (documentadas en la propia herramienta):** no se modela la posición exacta en metros sobre los 732 m / 2,400 ft de muelle (solo orden sur→norte + distancia en bahías físicas, ver "Anti-colisión aplicada" más abajo); el costo de mano de obra no encadena turnos para operaciones muy largas; discharge se asume siempre lleno; restows no aparecen como columna propia del Gantt; reducir cantidad de grúas a mitad de operación en un meal break sigue sin implementarse. **Probado de punta a punta con archivos reales (MAERSK SHIVLING, sep. 2026)** — el usuario encontró un falso "shipping line differs" al cargar sus propios archivos; la causa eran 3 bugs de parsing (detección de SSL basada en "último campo de UNB" en vez de un diccionario de tokens conocidos; nombre de buque que solo reconocía el calificador `:ZZZ:`; y un bloque `HAN+DIS` en el MOVINS de Maersk que el parser contaba como carga en vez de excluirlo por duplicar el discharge del BAPLIE) — los tres corregidos y reverificados contra los archivos reales del usuario. **UGT rediseñado (sep. 2026)** con la tabla "Horas de los Turno UGT" que el usuario entregó (un valor de garantía por hora de arranque, distinto de las tablas de costo) y verificado contra su ejemplo (grúa que termina 09:30, arranque 07:00 → UGT 1.5h). **Gantt corregido** para mostrar los Meal Breaks como una pausa real (la barra se detiene y reanuda con un conector punteado, ya no atraviesa el break) y **rediseñado en dos columnas** (Gantt a la izquierda más compacto, métricas a la derecha). **UGT corregido de nuevo (sep. 2026, mismo día):** la versión anterior anclaba la garantía a una sola ventana basada en la hora de inicio de la operación, así que cualquier operación que durara más que esa primera ventana (casi todas) mostraba 0.00h de UGT siempre — exactamente el bug que el usuario detectó con números reales. La regla correcta es una **cadena**: la ventana de garantía va rodando por los bloques estándar de la tabla "Horas de los Turno UGT" (saltando siempre al bloque con la hora de inicio más próxima después de que termina el anterior) hasta llegar al bloque donde cae el último movimiento de esa grúa — verificado al minuto exacto contra dos ejemplos reales del usuario. **Balanceo de grúas corregido también:** el reparto de bahías completas (sin dividir) dejaba a una grúa con 34% más tiempo de trabajo que otra cuando una sola bahía grande caía justo en el punto ideal de corte; ahora se puede dividir proporcionalmente una bahía frontera entre dos grúas vecinas (en momentos distintos) para balancear mejor — reduce el desbalance de 2 grúas a ~2%, aunque para 3-4 grúas queda una heurística local (no un óptimo global perfecto), documentado como tal. **Restows re-hechos (sep. 2026, mismo día):** antes se contaban como un bloque genérico repartido parejo entre las grúas al final de su turno. Ahora, si la lista de restow trae posición final (formato real de APMT "List Restow": Container No/DepPos/ArvPos/SzTp/Weight, código de posición de 6 dígitos bay(2)+row(2)+tier(2)), cada contenedor se agrega como una descarga real en su posición del BAPLIE (fuente de verdad, no el DepPos de la propia lista — se validó con datos reales que DepPos puede no coincidir con el BAPLIE) y una carga real en su ArvPos, dentro de la cola de esa bahía — visible en su propia columna del Gantt, no en un bloque "Restow" aparte. Si un contenedor de la lista no se encuentra en el BAPLIE, cae al comportamiento genérico anterior como respaldo. **Anti-colisión aplicada (sep. 2026, mismo día):** el usuario preguntó si la separación de seguridad entre grúas (regla del resumen original: mínimo 1 bahía de 40' entre grúas 1-2, 2 bahías entre cualquier otro par adyacente 2-6) realmente se respetaba, tras ver un escenario real con grúas 1 y 2 trabajando bahías 026/030 a la vez — la respuesta honesta fue que no, era coincidencia: el algoritmo de balanceo no tenía ninguna noción de posición física. El usuario pidió investigar la estructura/numeración real de bahías y usarla para que la regla se cumpla de verdad. Confirmado contra los datos reales ya procesados por la herramienta (BAPLIE de COSCO VIETNAM y MAERSK SHIVLING): la numeración estándar SMDG usa bahías pares como posiciones de 40' e impares como sub-posiciones de 20' a cada lado, y las posiciones físicas de 40' consecutivas incrementan de 4 en 4 (p. ej. 006, 010, 014, 018...) — los saltos más grandes en los datos reales coinciden con discontinuidades reales del casco, no con un margen de seguridad deliberado. Con eso, "1 paso físico de bahía" = diferencia de 4 en el número de bahía (`physicalBaySteps`, usando la bahía central de cada grupo fusionado). Se detectó que la función de balanceo anterior (`splitGroupByFraction`, sexta pasada) es incompatible con esta regla — dividir una misma bahía entre dos grúas adyacentes las deja a distancia física cero sin importar el horario — así que se eliminó; el balanceo ahora solo mueve bahías completas, y una nueva función `enforceCraneSpacing` corre después del balanceo (con prioridad sobre él) desplazando bahías completas hacia la grúa sur hasta cumplir el mínimo requerido, usando el par de números de grúa real (no la posición en el escenario) para decidir si aplica 1 o 2 bahías. Si el layout real de bahías de un buque no permite cumplirlo (verificado forzando 6 grúas sobre un buque con solo 6 grupos de bahía reales, sin margen para mover nada), la herramienta lo reporta como una nota roja en esa tarjeta de escenario en vez de asignar de forma insegura en silencio. Verificado con datos reales de COSCO VIETNAM (N=2/3/4): todos los márgenes cumplen (p. ej. N=4 con separaciones de 1/5/4 pasos físicos contra un mínimo de 1/2/2) y con una prueba adversarial manual (forzar un corte con solo 1 paso de separación donde se requerían 2 → la función movió una bahía completa y el margen subió a 5). **Restows generalizados a cualquier formato de export (sep. 2026, pasada novena):** el usuario reenvió el mismo criterio de restows con una captura mostrando una columna "Restow" (fallback genérico) en un escenario real — evidencia de que el matching por ID/posición de la séptima pasada no era lo bastante robusto para cualquier archivo. Se amplió el reconocimiento de encabezados (sinónimos para columna de ID de contenedor y posición final/inicial), se agregó detección por patrón de datos cuando el encabezado no es reconocible, se normalizan los IDs de contenedor (mayúsculas, sin espacios/guiones) en ambos lados de la comparación, y se agregó un fallback tolerante a dígito de verificación (prefijo de 10 caracteres, solo si es único en todo el buque). Verificado con 4 casos sintéticos sobre el BAPLIE real de COSCO VIETNAM (encabezados estándar, encabezados no convencionales, sin encabezados reconocibles, e IDs con variaciones de formato) — los 4 matchearon correctamente — y con una carga real vía la UI (`.xlsx` con encabezados no convencionales) que terminó con 0 restows en fallback. No se pudo reproducir el archivo exacto que el usuario vio fallar (no había un xlsx de restow disponible en disco esta sesión), así que si le sigue fallando con su archivo real, el siguiente paso es pedirle ese archivo específico. **Gantt responsivo y eje extendido (sep. 2026, pasada décima):** el usuario reportó barras de scroll horizontal/vertical en el diagrama y pidió columnas de bahía delgadas que siempre quepan, un eje de tiempo que llegue hasta el siguiente meal break después del último movimiento (no que corte justo ahí), y analizar si secuenciar el trabajo de popa a proa (en vez de proa a popa, el orden actual) reduce tiempo o mejora el balance. Se reescribió el ancho de columna para derivarse del ancho real medido del panel (en vez de un valor fijo), se eliminó el piso de px/minuto que podía hacer el SVG más alto que el máximo previsto, y se corrigió un bug real (no solo cosmético): la sección de resultados está oculta (`display:none`) hasta después de que el Gantt ya se había medido, así que el ancho leído siempre era 0 — corregido difiriendo la construcción del SVG con `setTimeout` (no `requestAnimationFrame`, que se suspende con la pestaña en segundo plano). Verificado con datos reales de COSCO VIETNAM en un panel angosto (~217px, columnas al mínimo, sin scroll) y en escritorio (~1440px, columnas a su ancho original). El eje ahora se extiende correctamente hasta el fin del siguiente meal break tras el último movimiento (verificado: última grúa termina 23:04, eje se extiende a 00:00). Sobre la dirección de trabajo: se analizó (no se cambió el código) corriendo el algoritmo de balanceo con el orden normal (proa→popa) y el invertido sobre datos reales — para N=2/3 es una imagen espejo exacta (mismo balance, ya que la simulación no modela tiempo de traslado de grúa entre bahías, así que el orden no afecta el tiempo total de ninguna grúa); para N=4 invertir resultó **peor** (34→184.8% de desbalance vs. 141.3% en el orden actual, dejando una grúa con solo 1.9 min de trabajo) por el sesgo direccional del algoritmo greedy de balanceo. Conclusión: no vale la pena cambiar el orden por defecto. **Prioridad de grúas por muelle (sep. 2026, pasada undécima):** el usuario entregó el orden real de asignación de grúas del Terminal — **muelle Sur**: Crane 3, 2, 1, 4, 5, 6; **muelle Norte**: Crane 4, 5, 6, 3, 2, 1 — reemplazando el criterio anterior (simplemente las N grúas de menor número). Excepción 2 (grúa de la prioridad fuera de servicio → se salta a la siguiente de la lista) queda implementada de forma natural al filtrar la lista de prioridad por `outOfService`; la excepción 1 (grúa sin altura suficiente → usar la grúa más cercana sin limitaciones para bajar la altura requerida) **no se implementó** — requiere el modelo de restricción de altura/alcance de tránsito de grúas (sección 5 del resumen original), que la herramienta nunca ha construido, documentado como brecha pendiente igual que antes. También se corrigió: **nunca se sugiere un escenario sin separación de seguridad entre grúas** — si el orden de trabajo proa→popa no la cumple para un conteo de grúas dado, se reintenta con el mismo conjunto de grúas pero secuencia popa→proa (mismo mecanismo de reversión analizado en la pasada décima, ahora también usado activamente para resolver violaciones, no solo evaluado); si ninguna de las dos direcciones lo logra, ese conteo de grúas se descarta de los 3 escenarios ofrecidos (o, si ningún conteo probado cumple, se muestra solo la opción menos mala en vez de dejar la página vacía). Verificado con datos reales de COSCO VIETNAM en ambos muelles: Sur eligió {2,3}/{1,2,3}/{1,2,3,4} para N=2/3/4 (coincide exactamente con los prefijos ordenados de la prioridad Sur), todos limpios en sentido proa→popa; Norte eligió {4,5}/{4,5,6} limpios, y **{3,4,5,6} para N=4 tenía una violación real en sentido proa→popa que se resolvió invirtiendo a popa→proa** — un caso real, no hipotético, del nuevo mecanismo funcionando. También se verificó el fallback por grúa fuera de servicio (p. ej. grúa 3 fuera en Sur → N=2 elige {1,2}, N=4 elige {1,2,4,5}). Ver memoria `quay-commander-project-summary` (pasadas octava a undécima) para el detalle completo, incluyendo qué queda fuera (excepción de altura, y reducir cantidad de grúas a mitad de operación en un meal break). |
| **Breakbulk Quote Desk** (BBK Calculator) | *No vive en este repo* — es un **Claude Artifact** (`https://claude.ai/artifact/VDFa7QSgiMKAfVuZui6eMd`), enlazado desde `TOOLS` en `index.html` con `external:true`. Calculadora de cotizaciones de carga breakbulk (BBK): el usuario registra una cotización (peso, dimensiones, ruta), la herramienta la valida contra una tabla de tarifas/referencias guardada (marcando ⚠ si hay datos de origen que se contradicen) y calcula factibilidad + precio; guarda todas las cotizaciones en el navegador (pestañas Quotes / New Quote / Reference & Rates) con export/import JSON para compartir entre equipos. Usa solo la capacidad `downloads` del runtime de Artifacts (no usa `db`) — por eso, a diferencia de SQDC, **no tiene la limitación de organization-internal**; cualquiera con el link puede usarla igual (el guardado es local a cada navegador, vía `localStorage`, no compartido). El artifact ya sigue el sistema de temas de la plataforma (`prefers-color-scheme` + `data-theme`) automáticamente, sin necesidad de un toggle propio. **Se le agregó (sep. 2026) un botón "← SMART Hub"** (`<a class="hub-back" href="https://al3xpinz0n.github.io/smarthub/">`) en la topbar, editando y republicando el artifact directamente (Version 2) — verificado visualmente que el botón aparece y el resto de la app sigue funcionando. |

Yard, Gate, Rail y VAS siguen vacías — cada una debe mostrar su estado vacío hasta que se agregue su primera herramienta real.

### Control de acceso por área

Cada área requiere contraseña para entrar:

- **Strategy** usa una contraseña propia y distinta al resto: **`Mobile.Strategy`**.
- Todas las demás áreas usan el patrón **`ABS.` + nombre del área** (p. ej. `ABS.Vessel`, `ABS.Gate`, `ABS.Yard`, `ABS.Rail`, `ABS.VAS`).

En `index.html`, `AREA_HASHES` guarda el **SHA-256** de cada contraseña (nunca el texto plano) y el gate la valida con `crypto.subtle.digest` antes de desbloquear esa área en `sessionStorage` para esa pestaña del navegador.

⚠️ **Esto es un control de acceso liviano, no seguridad real.** El sitio es 100% estático y el repo es público — cualquiera con el link puede leer el HTML/JS fuente (aunque no el texto plano de la contraseña, sí el esquema `ABS.<Área>` / `Mobile.Strategy`, fácil de adivinar). Sirve para mantener a visitantes casuales fuera de áreas que no les corresponden, **no** para proteger datos sensibles. Por eso la regla de la siguiente sección sigue siendo innegociable: nunca poner datos operativos reales detrás de este gate — solo herramientas que procesan archivos que el propio usuario carga en su navegador.

## Arquitectura técnica y seguridad (no negociable)

- **100% client-side, cero backend.** No hay servidores, bases de datos ni APIs recibiendo datos de la terminal. **Única excepción documentada:** `Yard Strategy` usa Firebase Firestore para compartir el estado del patio entre todos los que abren la app — decisión explícita del usuario (ver tabla de herramientas de Strategy arriba). No repetir este patrón en otra herramienta sin que el usuario lo pida de nuevo.
- Los archivos que carga el usuario en cada herramienta (`.xlsx`, `.csv`, `.txt`) se procesan **exclusivamente en memoria del navegador**. Los datos operativos de la terminal nunca deben viajar por internet ni guardarse en GitHub. (De nuevo, `Yard Strategy` es la excepción: la *configuración* de estrategia del patio sí viaja a Firestore — no datos de carga/buques reales.)
- **Nunca** subir al repo: API keys, contraseñas en texto plano, credenciales, tokens, ni archivos de datos reales/hardcodeados.
- Como el repo es público, cualquier cosa que se suba es visible para todo el mundo — extremar cuidado con lo anterior.

## Librerías JS sugeridas (para las herramientas nuevas)

| Librería | Función | Caso de uso |
|---|---|---|
| **SheetJS** (`xlsx.full.min.js`) | Leer/parsear Excel/CSV | Reportes N4/XPS, listas de descarga, planes de patio |
| **jsPDF** / **html2pdf.js** | Generar PDF | Entregables de turnos y handovers |
| **Chart.js** | Gráficas | Tendencias de CMPH, tiempos muertos, productividad |
| **JSZip** | Comprimir archivos | Descarga masiva de reportes en un `.zip` |

## Estructura de directorios objetivo

```text
smarthub/                            <-- repo GitHub (carpeta local sigue llamándose Company-Projects)
├── index.html                       <-- Hub principal (SMART Hub, ya existe)
├── README.md
├── CLAUDE.md                        <-- este archivo
├── assets/
│   └── fonts/                       <-- Maersk Text (.ttf) usadas por index.html
└── tools/
    ├── strategy/
    │   ├── yard-strategy/index.html
    │   └── export-empty-analysis/index.html
    ├── vessel/
    │   ├── vessel-move-reconciliation/index.html
    │   └── baplie-correction/index.html
    ├── yard/<nombre>/index.html
    ├── gate/<nombre>/index.html
    ├── rail/<nombre>/index.html
    └── vas/<nombre>/index.html
```

Cada herramienta nueva va en `tools/<área>/<nombre>/index.html`, autocontenida (HTML+CSS+JS en un solo archivo, siguiendo el mismo patrón que `index.html`), con **todo su texto en inglés** (ver "Idioma: inglés siempre") y debe mantener consistencia visual con el hub principal (revisar `index.html` y el skill `apm-terminals-brand` antes de diseñar cualquier UI nueva). Al terminarla, agregar su entrada al arreglo `TOOLS` de `index.html` con el `area` correcto y el `href` apuntando a esa carpeta.

## Cómo trabajar en este repo

- Cada herramienta es un archivo HTML autocontenido (sin build step, sin frameworks pesados) — coherente con el enfoque 100% estático del hub.
- Antes de crear una herramienta nueva, revisar `index.html` para mantener el mismo lenguaje visual (tarjetas, áreas operativas, paleta de colores APM, tipografía Maersk Text) y el mismo idioma (inglés).
- El usuario revisa y publica los cambios con GitHub Desktop (Commit to main → Push origin) — no asumir acceso a git ni terminal salvo que el propio Claude Code lo tenga disponible en esta máquina.
- Existen otros dos documentos de referencia en la raíz del repo: `GUIA-PORTAL-HARDCODEADO.md` y `SETUP-VSCODE-GITHUB-CLAUDE.md` (este último es un runbook de instalación de entorno más amplio, orientado a un stack PHP/Laravel que **no aplica** a este proyecto — ignorar esa parte salvo que el usuario indique lo contrario).

## Próximo paso acordado

Seguir construyendo herramientas reales por área (Yard, Gate, Rail, VAS todavía no tienen ninguna) y agregar su entrada a `TOOLS` en `index.html` con el `area` correcto al terminarlas. Cuando una herramienta ya exista como archivo HTML fuera del repo (como pasó con las cuatro ya incorporadas), copiarla a `tools/<área>/<nombre>/index.html` tal cual si ya sigue la marca APM (Maersk Text, paleta oficial, inglés) — no reescribirla sin necesidad. Si no sigue la marca al 100% (caso de `vessel-move-reconciliation`, ver arriba), incorporarla igual tal cual salvo que el usuario pida explícitamente alinearla.
