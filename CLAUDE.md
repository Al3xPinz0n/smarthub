# SMART Hub · APM Terminals Mobile

Este archivo se lee automáticamente al abrir este repositorio con Claude Code. Contiene el contexto completo del proyecto para que puedas continuar el trabajo sin perder lo ya avanzado.

## Resumen del proyecto

- **Nombre del portal:** **SMART Hub** (APM Terminals Mobile · Operational Intelligence Hub).
- **Propósito:** Portal centralizado estático (*client-side hub*) para alojar micro-herramientas operativas, simuladores y generadores de reportes para las operaciones de APM Terminals Mobile (Alabama), organizadas por **área operativa**.
- **Arquitectura de origen:** Inspirado en la estructura base de *BrainPort*; el contenido y las herramientas de ejemplo que traía esa base ya se eliminaron — el hub arranca vacío y cada área se llena a medida que se construye una herramienta real para APM Terminals Mobile.
- **Dueño:** Alex Pinzon (Operations / Strategy Team).

## Idioma: inglés siempre

**Todo el contenido visible de la plataforma (`index.html` y cada herramienta en `tools/`) debe estar en inglés americano profesional, con terminología de puertos y terminales de contenedores** (vessel, berth, yard, gate, rail, stowage, gate-in/gate-out, prime mover, etc.). No usar español en texto de UI, aunque el usuario escriba las instrucciones en español — traducir el contenido, no copiarlo literal. Comentarios de código y este `CLAUDE.md` sí pueden quedar en español (documentación interna para trabajar con Claude Code).

## Estado actual (confirmado y funcionando)

- **Repositorio:** `Strategy-Team-AL/smarthub` en GitHub — **público** (necesario para GitHub Pages gratis). Renombrado desde `Company-Projects` (sep. 2026) y luego **transferido de la cuenta personal (`Al3xPinz0n`) a la cuenta corporativa `Strategy-Team-AL`** (sep. 2026, historial completo preservado); el remoto local ya apunta a la nueva URL.
- **Sitio publicado:** `https://strategy-team-al.github.io/smarthub/`.
- **Despliegue:** GitHub Pages desde `Settings > Pages`, origen `Deploy from a branch`, rama `main`, directorio `/(root)`.
- **Repo local:** carpeta `C:\Users\ADP037\Documents\GitHub\Company-Projects` (el nombre de la carpeta local quedó igual; solo cambió el repo remoto) — GitHub Desktop debe re-autenticarse con la cuenta corporativa `Strategy-Team-AL` para poder hacer push (las credenciales previas apuntaban a la cuenta personal).
- **Flujo de publicación:** cualquier cambio en `main` se refleja automáticamente en el sitio (1-2 minutos) al hacer push — el usuario usa GitHub Desktop (Commit → Push) para esto, sin línea de comandos.
- **El hub arrancó vacío y se va llenando por herramienta real.** `TOOLS` en `index.html` ya no está vacío: **Strategy** tiene tres herramientas (`Yard Strategy`, `Export & Empty Analysis` y `SQDC Operational Review`) y **Vessel** tiene cuatro (`Vessel Move Reconciliation`, `Inbound BAPLIE Correction`, `Quay Commander` y `Breakbulk Quote Desk`), ver más abajo. Yard, Gate, Rail y VAS siguen mostrando su estado vacío ("No tools have been added...") hasta que se agregue su primera herramienta real.

## Diseño y marca

El hub sigue el skill `apm-terminals-brand` (colores, tipografía, logo e íconos oficiales de APM Terminals). **Revisar ese skill antes de tocar cualquier estilo o de construir una herramienta nueva.**

- **Tipografía:** Maersk Text (Light/Regular/Bold), cargada vía `@font-face` desde `assets/fonts/*.ttf`. Fallback: Arial.
- **Colores:** paleta oficial APM en `:root` de `index.html` — Orange `#FF6441`, Dark Gray `#3C3C46`, Aqua Green `#0A6E82`, Maersk Blue `#42B0D5`, Soft Yellow `#F0BE78`, grises `#5A6E7D` / `#AAB4C3` / `#E6EBF2`.
- **Logo:** SVG oficial embebido directamente en el `<header>` de `index.html`, a **236px de ancho** (2x el tamaño original — pedido explícito del usuario). No recrear, recolorear ni reducir el logo por debajo de esto sin que el usuario lo pida.
- **Íconos de área:** pictogramas oficiales de la familia `pictograms-orange.svg` del skill (tono naranja único, sin recoloreo), embebidos como sprite `<symbol>` al inicio de `index.html`. En la grilla de áreas se muestran a **85px** dentro de un contenedor de **150px** (2.5x el tamaño original — pedido explícito del usuario). Reutilizar ese mismo sprite para íconos nuevos en vez de emojis o íconos genéricos.
- **Cada herramienta debe tener, en su header: un botón "← SMART Hub" y un toggle de día/noche** (sep. 2026, pedido explícito del usuario, aplicado a las 5 herramientas existentes). Patrón estándar (copiar tal cual en herramientas nuevas):
  - Botón de vuelta: `<a href="../../../index.html">← SMART Hub</a>` (esa ruta relativa asume la profundidad `tools/<área>/<nombre>/index.html`; ajustar si la herramienta vive más profundo).
  - Toggle de tema: botón con dos `<svg>` (sol/luna) alternados vía CSS (`.icon-moon{display:none} html[data-theme="dark"] .icon-sun{display:none} html[data-theme="dark"] .icon-moon{display:block}`), que hace `document.documentElement.setAttribute('data-theme', 'dark'|'light')` y lo guarda en `localStorage` con una key **propia de cada herramienta** (no compartida) — p. ej. `yardstrategy-theme`, `exportempty-theme`, `vesselmove-theme`, `edi-tool-theme` (BAPLIE), `quaycommander-theme`. Un script mínimo en el `<head>`, antes de cualquier otro `<script>`, aplica el tema guardado (o `prefers-color-scheme`) antes del primer paint para evitar parpadeo.
  - El modo oscuro en sí se logra agregando un bloque `html[data-theme="dark"]{ --var: valor-oscuro; ... }` que sobreescribe las variables CSS que la herramienta ya usa en su `:root` (fondo/tarjeta/borde/texto) — no hace falta tocar cada regla individual si la herramienta ya está construida sobre variables CSS. Ver `tools/vessel/quay-commander/index.html` como referencia del patrón completo (fue la primera en implementarlo) y `tools/vessel/baplie-correction/index.html` (implementación independiente, ligeramente distinta pero equivalente).
  - Excepción conocida: `vessel-move-reconciliation` — el modo oscuro cubre el header y el fondo general, pero la zona de drag-and-drop de archivos (`.drop`) se quedó con fondo claro fijo; no se corrigió porque no formaba parte de lo pedido (solo agregar los botones), queda como ajuste visual menor pendiente si se retoma esa herramienta.

## Estructura de navegación: áreas operativas

El hub usa 6 tarjetas de **área operativa** en la página de inicio (no pestañas, no el término "gaveta" — ese fue solo un concepto usado para explicar la idea durante el diseño, nunca debe aparecer en el producto ni en el código). Cada desarrollo nuevo se agrega al área que le corresponda (arreglo `TOOLS` dentro de `index.html`, cada entrada con su `area` matching una de estas keys):

| Área     | Alcance |
|----------|---------|
| Strategy | Operational planning, performance targets, and ABS reporting for the Terminal |
| Vessel   | Vessels at berth, stowage planning, crane productivity |
| Yard     | Yard status, prime mover flow, operator performance |
| Gate     | Appointments, gate-in/gate-out processing, truck flow |
| Rail     | Rail operations scheduling, railcar movements |
| VAS      | Value-added services (stuffing, stripping, etc.) |

**Strategy** ya tiene sus dos primeras herramientas reales (construidas fuera de este repo y luego incorporadas):

| Herramienta | Carpeta | Qué hace |
|---|---|---|
| **Yard Strategy** | `tools/strategy/yard-strategy/` | Mapa interactivo de bloques del patio para definir y ajustar la estrategia de yard — clasificar bloques, asignar servicios/líneas y revisar capacidad por clase. Tiene su propio candado interno adicional para la pestaña "Configuration" (contraseña embebida en el JS del archivo, `StrategyMobile` — es de ese archivo, no del gate de SMART Hub; no lo confundas con `AREA_HASHES`). ⚠️ **Excepción a "100% client-side, cero backend":** esta es la única herramienta del hub con estado compartido real — usa **Firebase Firestore** (proyecto `apmt-mobile-smarthub`, cuenta del usuario, plan gratuito Spark) en vez de `localStorage`, para que un cambio guardado por cualquier persona desde Configuration se vea al instante en todas las pantallas que tengan la app abierta (verificado en vivo con dos pestañas). El objeto `FIREBASE_CONFIG` embebido en el JS es información pública (así es como Firebase espera que se distribuya su config web; la protección real está en las Firestore Security Rules, no en ocultar esas claves). Las Security Rules del proyecto están en modo abierto (`allow read, write: if true` para `yardStrategy/*`) — mismo espíritu que el resto de SMART Hub: un candado liviano (`StrategyMobile`), no autenticación real. Nunca subir datos operativos sensibles a este documento. |
| **Export & Empty Analysis** | `tools/strategy/export-empty-analysis/` | El usuario sube dos Excel (lista de movimientos de carga + detalle de visitas de buque) y la herramienta calcula TEUs de exportación por POD/servicio, perfil de peso y TEUs vacíos por línea, usando SheetJS 100% en el navegador. |
| **SQDC Operational Review** | *No vive en este repo* — es un **Claude Artifact** (`https://claude.ai/artifact/RvjdKHYqfj7DGKNXY2VEZS`), enlazado desde `TOOLS` en `index.html` con `external:true` (abre en pestaña nueva, `target="_blank"`). Revisión mensual de Safety/Quality/Delivery/Cost por buque: captura de datos SQDC, registro de acciones de la reunión (abiertas/cerradas), próximos buques/retos/oportunidades, log consolidado de acciones, biblioteca mensual SQDC, y generación de la presentación ejecutiva. Usa las capacidades `db` (guardado compartido de reportes/acciones) y `downloads` (exportar la presentación) del runtime de Artifacts — **por eso no se copió el HTML al repo**: fuera del entorno de Claude (`window.claude` no existe en una página servida por GitHub Pages), el guardado simplemente no funcionaría. ⚠️ Igual que con Yard Strategy/Firebase, la capacidad `db` es **organization-internal** — solo miembros con sesión iniciada en la organización de claude.ai del dueño pueden leer/escribir los datos compartidos; el equipo de APM Terminals Mobile no tiene esas cuentas (confirmado por el usuario). El usuario ya cambió el "Share" del artifact a **"Anyone with the link"** (estaba en "private" por defecto) — el resto del equipo puede abrirlo desde el link del hub; el guardado compartido (`db`) en sí sigue limitado a quien tenga sesión en la organización de claude.ai del dueño. |

**Vessel** tiene cuatro herramientas:

| Herramienta | Carpeta | Qué hace |
|---|---|---|
| **Vessel Move Reconciliation** | `tools/vessel/vessel-move-reconciliation/` | El usuario sube la lista de movimientos de contenedores de una escala de buque (xlsx/xls/csv) y la herramienta concilia discharge, load y restow por categoría de contenedor (Dry, Reefer, Tank, Flat Rack, Pallet Wide) y tamaño, desglosado por naviera. Usa SheetJS, 100% en el navegador. ⚠️ Esta herramienta usa la tipografía Google Fonts "Plus Jakarta Sans" y valores de color aproximados en vez de Maersk Text / la paleta exacta del skill `apm-terminals-brand` — se incorporó tal cual porque ya funcionaba y el usuario solo pidió agregarla; si en algún momento se quiere alinear 100% a la marca, es un ajuste pendiente. |
| **Inbound BAPLIE Correction** | `tools/vessel/baplie-correction/` | El usuario carga un EDI/BAPLIE de entrada; la herramienta corre validaciones obligatorias (bloquea si hay `+PHA+` o `TBD_` sin corregir), identifica automáticamente la naviera (SSL) por posición estructural en `UNB+`/`TDT+` (con banner de conflicto o selección manual si no hay match único), aplica **4 reglas generales que corren para las 8 navieras** (`LOC+9`: `XXVUT`→`VNVUT`, `XXCMT`→`VNCMT`, `NLDDN`→`NLRTM`; `NAD+` que termina en `:172:ZZZ`→`:172:20`) más las reglas de corrección propias de esa naviera (de las 8 soportadas: MAE, CMA, ZIM, COS, OCL, ONE, EGL, MSC), y genera el archivo corregido listo para el TOS. 100% en el navegador, sin backend. Actualizada (sep. 2026) para agregar esas 4 reglas generales — antes solo corrían las reglas específicas de cada SSL; verificado con casos sintéticos que las reglas generales y las de SSL se aplican juntas correctamente (incl. MSC, que antes no tocaba el archivo y ahora si las reglas generales aplican reporta el cambio en vez de "no change needed"). Documentación completa del spec: `Inbound_BAPLIE_Correction_Tool_Documentation.md` (en `C:\Users\ADP037\Downloads`, no está en este repo). Ya sigue la marca al 100% (Maersk Text embebido, paleta exacta, modo día/noche). |
| **Quay Commander** | `tools/vessel/quay-commander/` | Optimizador de split de grúas con diagrama de secuenciación tipo Quay Commander (N4). El usuario sube BAPLIE + MOVINS (drag-and-drop o click, con botón para **borrar los archivos cargados y empezar de nuevo**) y opcionalmente un listado de restows; elige fecha + **hora de arranque restringida a las horas canónicas de "Inicios de Operaciones"** (07:00, 08:00, 10:00, 13:00, 15:00, 19:00, 00:00); costado de atraque; **muelle Sur/Norte define el orden de prioridad de selección de grúas** (ya no es solo informativo, ver "Prioridad de grúas por muelle" más abajo — pasada undécima); grúa(s) fuera de servicio elegidas en un **dropdown** cuya primera opción es "All cranes available"; y un selector de **indicador prioritario** (MHPM & UGT, o PHS % & PMPH) que determina cuál par de KPIs pesa más al elegir el escenario recomendado (los otros KPIs quedan como criterio de desempate). No hay inputs de tarifa manual — el ingreso se calcula con la tarifa real de `APMT Cobra` (imágenes embebidas en esa pestaña del xlsx de costos, extraídas vía COM/Excel Shapes): lift rate por contenedor lleno/vacío × 20'-40' o 45', recargo OOG, recargo por grúas adicionales (+15% con 3 grúas, +20% con 4+, ya que la tarifa base incluye 2 grúas), tarifa de restow, e ingreso por Standby/Guarantee sobre el Total UGT. La herramienta simula el trabajo bahía-por-bahía (fusión impar-par-impar de bahías, detección de twin lift por peso/row/tier, regla de arranque de dual cycle en bodega —OOG primero, 1 row 100%, row siguiente a 75%, luego 42 moves/hr combinado—, contenedores OOG a 3.7 min/move vs 1.85 min/move regular, y las pausas de **Meal Break — 00:00-01:00, 05:50-07:00, 11:50-13:00 y 17:50-19:00**, sin operaciones durante esas 4 ventanas) para asignar bahías a grúas específicas (contiguo, en orden numérico sur→norte entre las grúas disponibles) y produce 3 escenarios (normalmente 1-3 grúas, hasta 4 si hace falta) con un **diagrama Gantt SVG por escenario** con columnas de bahía más angostas, símbolos ▲/▼ combinados (▼▲) cuando un bloque tiene descarga y carga a la vez, bandas rayadas en los Meal Breaks, y tooltip con tiempo/moves/twin lifts/dual cycle/OOG/breaks de esa grúa. **UGT se calcula por grúa individualmente**: para el último movimiento de CADA grúa se identifica el turno (de "Datos del Turno") que mejor cubre esa duración, y UGT = garantía de ese turno − tiempo trabajado. Los KPIs de la Sección 3: Total Moves en vez de "Cranes used"; **Cost/Revenue/Profit vs. recomendado son solo % (sin $), con semáforo por signo** (rojo si el costo es mayor, verde si el ingreso o la ganancia son mayores, no por umbral fijo); CI calculado con la fórmula real del Excel; MHPM con target ≤0.92. El reporte PDF es landscape estilo presentación (portada con recuadro destacado, pie de página con numeración de diapositiva en todas las páginas), con una diapositiva por escenario con su Gantt (SVG→PNG vía canvas), conclusión y notas de metodología actualizadas. **Fuentes:** `resumen_proyecto_quay_commander_mobile.md`, `Criterio de costos para las operaciones de buque.xlsx` (incl. imágenes de `APMT Cobra`), `Berths & Cranes.png` / `QC.png` (en `C:\Users\ADP037\Downloads`, no están en este repo). Sigue la excepción explícita del usuario a "nunca hardcodear datos reales" para las tarifas/costos laborales (ver memoria `quay-commander-crane-cost-criteria`). **Simplificaciones aún vigentes (documentadas en la propia herramienta):** no se modela la posición exacta en metros sobre los 732 m / 2,400 ft de muelle (solo orden sur→norte + distancia en bahías físicas, ver "Anti-colisión aplicada" más abajo); el costo de mano de obra no encadena turnos para operaciones muy largas; discharge se asume siempre lleno; restows no aparecen como columna propia del Gantt; reducir cantidad de grúas a mitad de operación en un meal break sigue sin implementarse. **Probado de punta a punta con archivos reales (MAERSK SHIVLING, sep. 2026)** — el usuario encontró un falso "shipping line differs" al cargar sus propios archivos; la causa eran 3 bugs de parsing (detección de SSL basada en "último campo de UNB" en vez de un diccionario de tokens conocidos; nombre de buque que solo reconocía el calificador `:ZZZ:`; y un bloque `HAN+DIS` en el MOVINS de Maersk que el parser contaba como carga en vez de excluirlo por duplicar el discharge del BAPLIE) — los tres corregidos y reverificados contra los archivos reales del usuario. **UGT rediseñado (sep. 2026)** con la tabla "Horas de los Turno UGT" que el usuario entregó (un valor de garantía por hora de arranque, distinto de las tablas de costo) y verificado contra su ejemplo (grúa que termina 09:30, arranque 07:00 → UGT 1.5h). **Gantt corregido** para mostrar los Meal Breaks como una pausa real (la barra se detiene y reanuda con un conector punteado, ya no atraviesa el break) y **rediseñado en dos columnas** (Gantt a la izquierda más compacto, métricas a la derecha). **UGT corregido de nuevo (sep. 2026, mismo día):** la versión anterior anclaba la garantía a una sola ventana basada en la hora de inicio de la operación, así que cualquier operación que durara más que esa primera ventana (casi todas) mostraba 0.00h de UGT siempre — exactamente el bug que el usuario detectó con números reales. La regla correcta es una **cadena**: la ventana de garantía va rodando por los bloques estándar de la tabla "Horas de los Turno UGT" (saltando siempre al bloque con la hora de inicio más próxima después de que termina el anterior) hasta llegar al bloque donde cae el último movimiento de esa grúa — verificado al minuto exacto contra dos ejemplos reales del usuario. **Balanceo de grúas corregido también:** el reparto de bahías completas (sin dividir) dejaba a una grúa con 34% más tiempo de trabajo que otra cuando una sola bahía grande caía justo en el punto ideal de corte; ahora se puede dividir proporcionalmente una bahía frontera entre dos grúas vecinas (en momentos distintos) para balancear mejor — reduce el desbalance de 2 grúas a ~2%, aunque para 3-4 grúas queda una heurística local (no un óptimo global perfecto), documentado como tal. **Restows re-hechos (sep. 2026, mismo día):** antes se contaban como un bloque genérico repartido parejo entre las grúas al final de su turno. Ahora, si la lista de restow trae posición final (formato real de APMT "List Restow": Container No/DepPos/ArvPos/SzTp/Weight, código de posición de 6 dígitos bay(2)+row(2)+tier(2)), cada contenedor se agrega como una descarga real en su posición del BAPLIE (fuente de verdad, no el DepPos de la propia lista — se validó con datos reales que DepPos puede no coincidir con el BAPLIE) y una carga real en su ArvPos, dentro de la cola de esa bahía — visible en su propia columna del Gantt, no en un bloque "Restow" aparte. Si un contenedor de la lista no se encuentra en el BAPLIE, cae al comportamiento genérico anterior como respaldo. **Anti-colisión aplicada (sep. 2026, mismo día):** el usuario preguntó si la separación de seguridad entre grúas (regla del resumen original: mínimo 1 bahía de 40' entre grúas 1-2, 2 bahías entre cualquier otro par adyacente 2-6) realmente se respetaba, tras ver un escenario real con grúas 1 y 2 trabajando bahías 026/030 a la vez — la respuesta honesta fue que no, era coincidencia: el algoritmo de balanceo no tenía ninguna noción de posición física. El usuario pidió investigar la estructura/numeración real de bahías y usarla para que la regla se cumpla de verdad. Confirmado contra los datos reales ya procesados por la herramienta (BAPLIE de COSCO VIETNAM y MAERSK SHIVLING): la numeración estándar SMDG usa bahías pares como posiciones de 40' e impares como sub-posiciones de 20' a cada lado, y las posiciones físicas de 40' consecutivas incrementan de 4 en 4 (p. ej. 006, 010, 014, 018...) — los saltos más grandes en los datos reales coinciden con discontinuidades reales del casco, no con un margen de seguridad deliberado. Con eso, "1 paso físico de bahía" = diferencia de 4 en el número de bahía (`physicalBaySteps`, usando la bahía central de cada grupo fusionado). Se detectó que la función de balanceo anterior (`splitGroupByFraction`, sexta pasada) es incompatible con esta regla — dividir una misma bahía entre dos grúas adyacentes las deja a distancia física cero sin importar el horario — así que se eliminó; el balanceo ahora solo mueve bahías completas, y una nueva función `enforceCraneSpacing` corre después del balanceo (con prioridad sobre él) desplazando bahías completas hacia la grúa sur hasta cumplir el mínimo requerido, usando el par de números de grúa real (no la posición en el escenario) para decidir si aplica 1 o 2 bahías. Si el layout real de bahías de un buque no permite cumplirlo (verificado forzando 6 grúas sobre un buque con solo 6 grupos de bahía reales, sin margen para mover nada), la herramienta lo reporta como una nota roja en esa tarjeta de escenario en vez de asignar de forma insegura en silencio. Verificado con datos reales de COSCO VIETNAM (N=2/3/4): todos los márgenes cumplen (p. ej. N=4 con separaciones de 1/5/4 pasos físicos contra un mínimo de 1/2/2) y con una prueba adversarial manual (forzar un corte con solo 1 paso de separación donde se requerían 2 → la función movió una bahía completa y el margen subió a 5). **Restows generalizados a cualquier formato de export (sep. 2026, pasada novena):** el usuario reenvió el mismo criterio de restows con una captura mostrando una columna "Restow" (fallback genérico) en un escenario real — evidencia de que el matching por ID/posición de la séptima pasada no era lo bastante robusto para cualquier archivo. Se amplió el reconocimiento de encabezados (sinónimos para columna de ID de contenedor y posición final/inicial), se agregó detección por patrón de datos cuando el encabezado no es reconocible, se normalizan los IDs de contenedor (mayúsculas, sin espacios/guiones) en ambos lados de la comparación, y se agregó un fallback tolerante a dígito de verificación (prefijo de 10 caracteres, solo si es único en todo el buque). Verificado con 4 casos sintéticos sobre el BAPLIE real de COSCO VIETNAM (encabezados estándar, encabezados no convencionales, sin encabezados reconocibles, e IDs con variaciones de formato) — los 4 matchearon correctamente — y con una carga real vía la UI (`.xlsx` con encabezados no convencionales) que terminó con 0 restows en fallback. No se pudo reproducir el archivo exacto que el usuario vio fallar (no había un xlsx de restow disponible en disco esta sesión), así que si le sigue fallando con su archivo real, el siguiente paso es pedirle ese archivo específico. **Gantt responsivo y eje extendido (sep. 2026, pasada décima):** el usuario reportó barras de scroll horizontal/vertical en el diagrama y pidió columnas de bahía delgadas que siempre quepan, un eje de tiempo que llegue hasta el siguiente meal break después del último movimiento (no que corte justo ahí), y analizar si secuenciar el trabajo de popa a proa (en vez de proa a popa, el orden actual) reduce tiempo o mejora el balance. Se reescribió el ancho de columna para derivarse del ancho real medido del panel (en vez de un valor fijo), se eliminó el piso de px/minuto que podía hacer el SVG más alto que el máximo previsto, y se corrigió un bug real (no solo cosmético): la sección de resultados está oculta (`display:none`) hasta después de que el Gantt ya se había medido, así que el ancho leído siempre era 0 — corregido difiriendo la construcción del SVG con `setTimeout` (no `requestAnimationFrame`, que se suspende con la pestaña en segundo plano). Verificado con datos reales de COSCO VIETNAM en un panel angosto (~217px, columnas al mínimo, sin scroll) y en escritorio (~1440px, columnas a su ancho original). El eje ahora se extiende correctamente hasta el fin del siguiente meal break tras el último movimiento (verificado: última grúa termina 23:04, eje se extiende a 00:00). Sobre la dirección de trabajo: se analizó (no se cambió el código) corriendo el algoritmo de balanceo con el orden normal (proa→popa) y el invertido sobre datos reales — para N=2/3 es una imagen espejo exacta (mismo balance, ya que la simulación no modela tiempo de traslado de grúa entre bahías, así que el orden no afecta el tiempo total de ninguna grúa); para N=4 invertir resultó **peor** (34→184.8% de desbalance vs. 141.3% en el orden actual, dejando una grúa con solo 1.9 min de trabajo) por el sesgo direccional del algoritmo greedy de balanceo. Conclusión: no vale la pena cambiar el orden por defecto. **Prioridad de grúas por muelle (sep. 2026, pasada undécima):** el usuario entregó el orden real de asignación de grúas del Terminal — **muelle Sur**: Crane 3, 2, 1, 4, 5, 6; **muelle Norte**: Crane 4, 5, 6, 3, 2, 1 — reemplazando el criterio anterior (simplemente las N grúas de menor número). Excepción 2 (grúa de la prioridad fuera de servicio → se salta a la siguiente de la lista) queda implementada de forma natural al filtrar la lista de prioridad por `outOfService`; la excepción 1 (grúa sin altura suficiente → usar la grúa más cercana sin limitaciones para bajar la altura requerida) **no se implementó** — requiere el modelo de restricción de altura/alcance de tránsito de grúas (sección 5 del resumen original), que la herramienta nunca ha construido, documentado como brecha pendiente igual que antes. También se corrigió: **nunca se sugiere un escenario sin separación de seguridad entre grúas** — si el orden de trabajo proa→popa no la cumple para un conteo de grúas dado, se reintenta con el mismo conjunto de grúas pero secuencia popa→proa (mismo mecanismo de reversión analizado en la pasada décima, ahora también usado activamente para resolver violaciones, no solo evaluado); si ninguna de las dos direcciones lo logra, ese conteo de grúas se descarta de los 3 escenarios ofrecidos (o, si ningún conteo probado cumple, se muestra solo la opción menos mala en vez de dejar la página vacía). Verificado con datos reales de COSCO VIETNAM en ambos muelles: Sur eligió {2,3}/{1,2,3}/{1,2,3,4} para N=2/3/4 (coincide exactamente con los prefijos ordenados de la prioridad Sur), todos limpios en sentido proa→popa; Norte eligió {4,5}/{4,5,6} limpios, y **{3,4,5,6} para N=4 tenía una violación real en sentido proa→popa que se resolvió invirtiendo a popa→proa** — un caso real, no hipotético, del nuevo mecanismo funcionando. También se verificó el fallback por grúa fuera de servicio (p. ej. grúa 3 fuera en Sur → N=2 elige {1,2}, N=4 elige {1,2,4,5}). **Separación anti-colisión pasó a ser en base al tiempo real, no al rango estático (sep. 2026, pasada duodécima):** el usuario inició un proceso de revisión conjunta cargando archivos reales de un buque nuevo (MAERSK TANGIER, 631E/637W) para ir entrenando los criterios de distribución de grúas — dijo que seguirá cargando operaciones para revisar juntos, tratar como proceso continuo, no algo puntual. En el primer caso revisado (escenario Sur de 2 grúas) la herramienta dio un desbalance real y grande: 16.30h vs 5.49h, porque la verificación estática de separación (pasada octava) exige que los RANGOS asignados a cada grúa estén siempre a la distancia mínima, forzando que el grupo de bahía más pesado de toda la operación (045-047, 320 min) saltara completo a una sola grúa aunque el punto de corte ideal por balance estaba justo al lado. El usuario corrigió el criterio con un ejemplo real: la regla de separación debe aplicarse según si las dos grúas estarían físicamente cerca **al mismo tiempo**, no según si sus rangos asignados están cerca en el mapa — si una grúa termina su trabajo cerca del límite mucho antes de que la otra llegue ahí, no hay riesgo real. Verificado exactamente con los datos reales: el bloque "042" de una grúa corría 20:08–20:38 y el bloque "045-047" de la otra corría 07:20–13:50 — cero traslape de tiempo, así que marcarlo como inseguro era un error. Se reescribió la verificación (`findSpacingConflicts`) para solo marcar un conflicto cuando la distancia física Y el traslape de tiempo (usando los segmentos reales de trabajo, ya partidos por los meal breaks) ocurren juntos; `computeScenario` ahora simula primero la partición óptima por balance (sin ajuste estático previo) y solo cae al ajuste estático de `enforceCraneSpacing` (desplazar bahías completas) si aparece un conflicto real. Resultado verificado con los datos reales de TANGIER MAERSK: el escenario Sur de 2 grúas pasó de **16.30h/5.49h a 10.97h/10.82h**, y el tiempo operacional bajó de 19.80h a 13.30h — mejora grande y real, no cosmética. Sin regresión en COSCO VIETNAM (mismos números que pasadas anteriores) ni en el caso límite de 6 grúas sobre 6 grupos reales (sigue reportando los 2 conflictos correctamente, sin falso negativo). No se modela: tiempo de traslado de grúa entre bahías (sigue instantáneo), posición exacta en metros del muelle. **Reparto seguro de una misma bahía entre grúas vecinas, reintroducido (sep. 2026, misma sesión, pasada decimotercera):** siguiendo con la revisión conjunta, el usuario señaló que en el escenario de 3 grúas del mismo TANGIER MAERSK (grúa1 6.04h / grúa2 10.26h / grúa3 5.49h, grúa2 terminando 03:55 AM) la grúa 3 pudo haber empezado en la bahía 46, trabajado parte de esa bahía, y luego completado 058/062 hasta cerca de medianoche — reduciendo la carga de la grúa 2. Esto es exactamente la función de "compartir una bahía entre grúas adyacentes" que se había eliminado en la octava pasada por insegura (distancia física cero) — pero bajo el modelo de separación por tiempo real (pasada duodécima), compartir SÍ es seguro si una grúa termina su parte y se aleja mucho antes de que la otra llegue a la suya. Se reintrodujo `splitGroupByFraction` + una nueva función `refineBalanceWithSplits` que, después de que la asignación por grupos completos ya es segura, intenta dividir el grupo de la frontera entre dos grúas vecinas — pero solo mantiene la división si al re-simular el escenario completo sigue sin conflictos reales de separación (`findSpacingConflicts`). Verificado con datos reales: el caso de 3 grúas de TANGIER MAERSK pasó de **6.04h/10.26h/5.49h a 7.25h/7.45h/7.09h** (balance casi perfecto) y el tiempo operacional bajó de ~12.6h a **9.78h** — el algoritmo encontró algo mejor que la sugerencia literal del usuario (dividió tanto la frontera 1↔2 como la 2↔3). Sin regresión en N=2/N=4 (sin cambios, ya eran casi óptimos) ni en COSCO VIETNAM (N=2 mejoró como bono extra, N=3/N=4 sin cambios) ni en el caso límite de 6 grúas sobre 6 grupos (sigue reportando sus 2 conflictos reales correctamente). El usuario planteó esto explícitamente como un proceso continuo de entrenamiento — seguirá cargando archivos de operaciones reales para revisar juntos; cada ronda hasta ahora ha detectado una brecha real de modelado, no solo ajuste de parámetros. **UGT corregido de nuevo — la tabla "Horas de los Turno UGT" no era el criterio correcto — y nueva sugerencia de costo ET vs. break (sep. 2026, misma sesión, pasada decimocuarta):** el usuario marcó que el Total UGT mostrado (14.54h) para el mismo escenario de 3 grúas era incorrecto, y dio su propio cálculo por grúa (6min, 4h43min, 15min = 5h04min total) medido contra el **próximo meal break de cada grúa**, no contra la tabla "Horas de los Turno UGT" (que sí se transcribió bien desde el Excel, solo que resultó no ser el criterio correcto para esto). Se reescribió `ugtWindowEnd` para calcular tiempo hasta el inicio del próximo meal break — verificado: pasó de 14.54h a **5.04h**, coincidiendo casi al minuto con el cálculo del usuario. Además, el usuario pidió que cuando el UGT sea alto porque una grúa regresa de un break para muy poco trabajo (su ejemplo: grúa 2 vuelve de un break de 70 min para solo 7 min de trabajo), la herramienta sugiera trabajar durante el break en su lugar, comparando el costo de Extra Time (columnas ET+1 a ET+4 reales de "Costos por posición y turno": $1,852.08/hora para 1 grúa, tarifa derivada para 2-4 grúas por la misma proporción de headcount que ya usa la tabla de costos principal) contra el costo de turno extra por tomar el break — el usuario eligió explícitamente comparar solo costo de mano de obra (no ingreso de Standby). Verificado con el caso real: ET+1 ($4,745.96) vs. costo de turno extra ($4,976.48) → ahorro de ~$230.52, mostrado como sugerencia (💡) en la tarjeta del escenario, sin reescribir automáticamente el Gantt. **Sugerencia de ET vs. break corregida y con overlay visual (sep. 2026, misma sesión, pasada decimoquinta):** el usuario re-probó con los mismos archivos de TANGIER MAERSK (esta vez con sus nombres reales — Inbound Baplie .edi / MOVINS.edi / RESTOW LIST.xlsx, confirmado idéntico byte a byte) y pidió 2 correcciones a la sugerencia de la pasada anterior: (1) **visual** — cuando conviene trabajar durante el break, sobreponer la cola de trabajo de esa grúa sobre la franja del meal break en el Gantt, no solo describirlo en texto; (2) **atribución de costo/UGT** — el costo ET+1 debe aplicar solo a la grúa que sigue operando (su propia cuadrilla), no a la tarifa de todo el escenario (bug real: usaba la tarifa de N grúas del escenario en vez de 1 sola — el ahorro real pasó de ~$230 a **~$3,124** al corregirlo), y el UGT de esa grúa bajo la sugerencia debe ser el tiempo restante hasta el FIN del break original (no una hora nueva contada desde donde termina el trabajo real) — verificado: grúa 2 termina su trabajo real a las 23:57:01, el break original termina 01:00:00 → UGT sugerido = **1.05h**, coincidiendo con "hasta la 1:00 am". El overlay se dibuja en el Gantt como un recuadro punteado semi-transparente del color de esa grúa, exactamente sobre la ventana del break, en la columna de bahía correspondiente — sigue siendo solo una sugerencia visual, no reescribe el schedule real ni las demás métricas del escenario (costo, UGT total, etc. siguen reflejando el break tomado). Sin regresión en COSCO VIETNAM. **Dos bugs reales corregidos a partir del Gantt de la pasada anterior (sep. 2026, misma sesión, pasada decimosexta):** (1) el bracket de ET estaba estructuralmente limitado a ET+1 siempre — la fórmula usaba el tiempo restante de TODO el turno de la grúa (incluyendo bahías posteriores no relacionadas) y exigía que fuera menor al break mismo (~60-70 min), haciendo matemáticamente imposible llegar a ET+1.5 o más. El usuario lo detectó con un Gantt real de 4 grúas donde varias seguían trabajando hasta casi las 14:00 tras un break de 11:50-13:00, y correctamente identificó que eso requeriría ET+2, no ET+1. Corregido: el remanente ahora se calcula por bahía interrumpida específica (no todo el turno restante) y sin tope artificial — verificado con datos reales: una grúa con 171.9 min de remanente real ahora cae correctamente en **ET+3** ($5,556.24) en vez de quedar oculta o mal cotizada. (2) La asignación de grúa a bahía ignoraba la orientación real del buque en el muelle — el usuario mostró con el mapa real que, para el muelle Sur con el buque port side alongside, la proa debe trabajarla la grúa 3 (no la 2). Se derivó la regla geométrica general (con el muelle corriendo norte-sur y el agua al este: port-side-alongside → proa apunta al NORTE; starboard-alongside → proa apunta al SUR — misma regla para ambos muelles, es geometría del muelle, no algo distinto por muelle) e implementado en `computeScenario`: antes de simular, si la proa corresponde al extremo norte del conjunto de grúas seleccionado, se invierte el arreglo `craneNumbers` (ya ordenado ascendente = orden físico) una sola vez, sin tocar el resto de la lógica de simulación/separación/balanceo (todas indexan `craneNumbers[i]` ↔ `buckets[i]`). Verificado en las 4 combinaciones muelle×costado con datos reales de COSCO VIETNAM, incluyendo que esta inversión compone correctamente con la inversión de dirección por separación (pasada duodécima) cuando ambas aplican a la vez. **El "fix" de ET de la pasada anterior era en sí incorrecto — ET es un período continuo desde el primer break hasta el término real, no un bracket por break (sep. 2026, misma sesión, pasada decimoséptima):** el usuario mostró la captura real del mismo escenario de 2 grúas de TANGIER MAERSK con 4 sugerencias 💡 (2 por grúa) que parecían baratas — pero explicó que el reloj de ET empieza a correr desde el PRIMER break que se cruza y sigue corriendo sin reiniciarse hasta el último movimiento real (grúa 3 termina 20:38, primer break ~11:50 → casi 8h48min de ET continuo), y que más allá de ET+4 (4h) la tarifa no sube más ("el valor es el mismo") pero sigue acumulando — haciendo esto carísimo en casos así ("más de 20 horas a valor de ET+4"). Regla explícita del usuario: solo sugerirlo cuando la grúa esté genuinamente cerca de terminar, no porque el remanente de UN break aislado sea corto mientras quedan horas de trabajo real después. Se reescribió `findBreakAdvisories` para calcular UN período continuo por grúa (desde el primer break hasta su término real), tarifa plana sin brackets (se eliminó el redondeo a ET+1/+2/etc., ya que la tarifa no cambia más allá de 4h), y el costo de "tomar los breaks" ahora suma la duración de TODOS los breaks cruzados, no solo uno. Verificado exactamente con el escenario real de la captura del usuario: ambas grúas necesitan ~8.7-8.8h continuas de ET para terminar → ahora correctamente **sin sugerencias** (antes mostraba 4 sugerencias engañosas). Verificado también que el mecanismo sigue funcionando para un caso genuinamente corto (10 min de remanente real → sugerido, ahorro ~$2,507) y se mantiene en silencio para uno largo (6h10min restantes → no sugerido). Sin regresión en COSCO VIETNAM. **Grúas ahora terminan casi simultáneamente — regla de oro del resumen original (sep. 2026, misma sesión, pasada decimoctava):** el usuario mostró una captura real de 3 grúas terminando con 22 minutos de diferencia entre sí (16:35/16:45/16:57) y preguntó por qué no se balanceó para que terminaran juntas. Causa: el criterio de aceptación del ajuste fino comparaba minutos de trabajo, no el tiempo de término REAL simulado de cada grúa — estos pueden diferir si una grúa cruza distinta cantidad/duración de meal breaks que su vecina para el mismo trabajo real. Se cambió el criterio para comparar directamente `finish` de ambas grúas (ya se simulaban de todas formas para el chequeo de seguridad). Además, los umbrales de "no vale la pena intentar" (15 min de diferencia mínima, 8% de fracción mínima, solo 4 pasadas) eran demasiado conservadores — se bajaron a 2 min, 2%, 5 min de tamaño mínimo de grupo donante, y 20 pasadas. Verificado con el escenario real exacto de la captura: pasó de 22 minutos de diferencia a **menos de 2 minutos** entre las 3 grúas. Sin regresión en COSCO VIETNAM (N=2 mejoró aún más; N=3/N=4 se mantienen con brechas de varias horas — no por el algoritmo, sino porque la distribución de carga de ese buque específico no tiene suficiente flexibilidad de bahías para dividir entre 3-4 grúas, ahora documentado explícitamente como limitación de datos, no del algoritmo). **Búsqueda de fracción más agresiva, y corrección del diagnóstico de "muro de seguridad" (sep. 2026, misma sesión, pasada decimonovena):** a pedido del usuario de "explorar versiones más agresivas" sobre la brecha de 140 min en MAERSK SHIVLING N=4, se encontró que el ajuste fino solo probaba UNA fracción de corte por frontera y se rendía si esa fallaba en seguridad — nunca probaba otras. Se cambió a una búsqueda real (5% a 95% en pasos de 5%) que prueba varias fracciones y elige la mejor segura. Pero al investigar más a fondo se descubrió que el diagnóstico de "muro de seguridad" de la pasada anterior era, en parte, incorrecto: una vez que las otras fronteras (4↔3 y 2↔1) también se optimizan, la frontera 3↔2 resulta segura en TODO el rango de fracciones — lo que realmente limita el cierre de la brecha es que el grupo de bahía compartido (046, ~128 min) simplemente no es lo bastante grande para cerrar una brecha de ~130-150 min por sí solo (mover el 100% del grupo entre grúas solo cambia la brecha ~26 min, por un efecto no lineal: cambia cuántos meal breaks cruza la grúa receptora después). Es un límite estructural (cuánto puede mover una sola bahía compartida), no un trade-off de seguridad como se había explicado antes — corregido con el usuario. Resultado real: MAERSK SHIVLING N=4 mejoró modestamente (140.4→137.3 min, consistente con el margen real disponible en esa frontera). TANGIER MAERSK N=3 se mantiene excelente (4.9 min). Sin regresión en COSCO VIETNAM. Sigue pendiente (no abordado esta pasada): un optimizador realmente global que negocie entre más de una frontera a la vez — el siguiente nivel si se quiere seguir cerrando este caso específico. **Optimización global real probada — empeoró el resultado, revertida (sep. 2026, misma sesión, pasada vigésima):** a pedido explícito de "explorar una optimización real", se cambió el criterio de aceptación del ajuste fino de "¿mejora la brecha de ESTE par de grúas?" a "¿mejora la dispersión de TODO el escenario (máx−mín entre las 4 grúas)?", para permitir que la carga fluyera a través de más de una frontera a la vez. Resultado real en MAERSK SHIVLING N=4: la brecha **empeoró de 137.3 a 308.9 minutos** (más del doble). Causa: la grúa4 (el cuello de botella real) se drenaba agresivamente hacia la grúa3 porque eso siempre se veía bien "localmente considerado global", pero la grúa3 solo puede pasar una fracción mínima hacia la grúa2 por su propia frontera restringida (la misma zona angosta 042-046-050 de la pasada anterior) — el resultado fue que la grúa3 terminó más sobrecargada (9.36h) que lo que la grúa4 tenía permitido antes (11.96h se repartía a ~8.6h/8.6h entre grúa4 y 3 con el algoritmo anterior), mientras que grúa2/1 casi no se movieron. Una estrategia greedy de "siempre drenar el máximo actual" sin considerar cuánta carga puede realmente atravesar TODA la cadena de fronteras simplemente reubica el cuello de botella un salto más adelante, y puede empeorarlo. Se revirtió limpiamente al criterio por par (pasada decimonovena), re-verificado que restaura los 137.3 min exactos. Una optimización global real necesitaría razonamiento de capacidad de flujo (cuánto puede atravesar TODA la cadena, no solo el siguiente salto) — esencialmente un problema de flujo en redes / programación lineal pequeña, un desarrollo bastante más grande que una exploración de una sesión, no abordado. Documentado directamente en el comentario del código en el punto exacto donde se intentó y falló, para que una pasada futura no reintente el mismo enfoque roto desde cero. **Enfoque de flujo en redes real implementado (sep. 2026, misma sesión, pasada vigésimo primera):** a pedido explícito de "intenta con un enfoque de flujo en redes real", se implementó `refineBalanceAcrossChain` — a diferencia del intento fallido anterior (que dejaba que una grúa intermedia absorbiera la transferencia sin poder pasarla), esta trata el camino entre la grúa más ocupada y la que termina más temprano como una **cadena de eslabones que deben moverse juntos**: para cada frontera en ese camino, dona desde el lado más cercano a la grúa ocupada, usando la MISMA fracción en cada eslabón en un solo movimiento combinado (para que una grúa intermedia entregue aproximadamente lo mismo que recibe), simula el resultado completo, verifica seguridad en todas las fronteras, y solo acepta si la dispersión total del escenario realmente se reduce. Verificado que evita correctamente la falla de la pasada anterior (se negó a fabricar un movimiento cuando el ítem donante en la frontera grúa3↔grúa2 ya estaba agotado). El hallazgo más profundo: la pasada por pares (que corre primero) ya había llevado esa frontera compartida ("046") a solo ~1.9 min de estar completamente agotada — confirmado independientemente barriendo todo el rango de fracciones seguras de esa frontera (0-100%, todas seguras una vez que las otras fronteras también convergen). Como ese es el ÚNICO camino entre la grúa más ocupada y la más temprana para N=4 en este buque, y ya está al máximo, el algoritmo determinó correctamente que **no hay más margen disponible** — una confirmación real (no un supuesto) de que 137.3 min está muy cerca del óptimo verdadero para este caso específico dadas las restricciones de seguridad. Resultado: MAERSK SHIVLING N=4 sin cambio (137.3 min, ahora confirmado como límite real en vez de asumido), sin regresión en TANGIER MAERSK ni COSCO VIETNAM. Tiempo de generación subió de ~830ms a ~2000ms (sigue siendo aceptable para un clic de UI). **Sugerencia de ET corregida de nuevo — solo el último break cuenta, y se compara contra un turno nuevo real, no contra la extrapolación del turno vigente (sep. 2026, misma sesión, pasada vigésimo segunda):** el usuario cargó un buque nuevo (ONE HOUSTON, primera naviera ONE procesada por la herramienta) para seguir el proceso continuo de revisión y notó que el escenario de 4 grúas sí sugería correctamente continuar durante el meal break de la grúa 2, pero el de 2 grúas (operación de ~20h, cruzando 4 breaks con horas de trabajo real entre cada uno) no sugería nada — aunque esa operación también terminaba justo sobre su ÚLTIMO break. El usuario aclaró el criterio: ET solo debe evaluarse cuando la grúa está genuinamente cerca de terminar (su último break), comparado contra el costo real de pagar un turno nuevo en vez de seguir con el actual. Causa real: la pasada decimoséptima anclaba el período de ET al PRIMER break cruzado (correcto para 1-2 breaks pegados al final, pero daba ~20h absurdas aquí con 4 breaks separados por trabajo real) y comparaba contra la extrapolación suave del turno vigente en vez de contra lo que realmente cuesta reanudar — como los meal breaks terminan justo en horas canónicas de "Inicios de Operaciones" (07:00, 13:00, 19:00, 00:00), retomar después de un break casi siempre implica llamar un turno nuevo completo con su propia garantía mínima, no una extensión barata del turno actual. Se reescribió `findBreakAdvisories`: (1) solo el ÚLTIMO break de cada grúa es candidato a ET; (2) el costo de comparación es ahora un turno nuevo real (`nearestShiftFamily`+`selectShiftCost` a la hora de reanudo, dimensionado al remanente de esa grúa), no la extrapolación del turno vigente. Verificado con datos reales de ONE HOUSTON N=2: grúa 2 ET $3,740 vs. turno nuevo (WS2) $11,991 (ahorro ~$8,250); grúa 3 similar (~$8,151) — coincide exactamente con la expectativa del usuario. El N=4 que ya funcionaba también mejoró: mismo costo ET ($4,505), pero el turno nuevo real es $22,656 (antes comparaba contra $6,608 de extrapolación) — ahorro real ~$18,151, no ~$2,103. Sin regresión en balance/spacing de TANGIER MAERSK ni COSCO VIETNAM (esta pasada solo tocó la economía de la sugerencia, no la asignación de grúas). De paso, el N=4 de ONE HOUSTON (487 min de desbalance, solo 8 grupos de bahía reales, uno de 763 min) se investigó y confirmó como límite estructural genuino: con tan pocos grupos, varias grúas terminan con un único grupo enorme cada una, y compartirlo con la vecina resulta inseguro porque ambas arrancarían esa misma bahía en el mismo instante, sin nada en cola antes. **Sugerencia de ET pasada a formato porcentual, y nueva sugerencia de "liberar grúa a mitad de operación" (sep. 2026, misma sesión, pasada vigésimo tercera):** el usuario pidió dos cosas sobre el caso real de ONE HOUSTON N=4: (1) que la sugerencia de ET ya no muestre montos en dólares, solo el % de diferencia en costo; (2) razonando directamente sobre los números reales de ese escenario, señaló que la mejor optimización de costo disponible ahí es liberar definitivamente la cuadrilla de la Grúa 1 en su propio último meal break (en vez de traerla de vuelta) y dejar que la Grúa 2 — que ya había terminado su propio bay-group y estaba inactiva desde mucho antes de ese break — absorba lo que le quedaba a la Grúa 1, evitando el sobrecosto de ET y aprovechando el tiempo muerto de la Grúa 2. **Parte 1:** `findBreakAdvisories` ahora expone `savingsPct` y el texto en pantalla quedó como "cuesta N% menos que un turno nuevo", sin cifras en dólares. **Parte 2, verificada con los datos reales antes de escribir código:** con la consola del navegador se confirmó que la Grúa 3 (cuello de botella real, un solo bay-group de 763 min) termina última a las 22:23 local; Grúa 4 a las 20:33; Grúa 1 a las 20:03 (cruza el mismo break de 17:50-19:00 que 3 y 4, con solo ~63 min reales de trabajo después); y la Grúa 2 (bay-group 046) solo cruza el break de mediodía y termina a las 14:16 — casi 6 horas antes de que la Grúa 1 siquiera llegue a su propio break. Se construyó `findCraneConsolidationAdvisories`, modelando la alternativa como dos turnos consecutivos — reutilizando la misma convención ya validada por el usuario en la pasada anterior (reanudar tras un break = turno nuevo en hora canónica): Segmento 1 = las N grúas originales, desde la hora de arranque hasta el break de la grúa que se retira; Segmento 2 = turno nuevo de N-1 grúas, desde la hora de reanudo hasta el fin real del escenario (que la grúa retirada nunca bloqueaba, por construcción). Se detectó y resolvió un empate real: la Grúa 1 y la Grúa 3 cruzan su último break exactamente a la misma hora (17:50) y ambas alternativas (retirar 1 o retirar 3, con la Grúa 2 absorbiendo) dan matemáticamente el mismo costo total ($69,440.51) aunque "liberar la grúa cuello de botella de 763 min" es una acción mucho más disruptiva en la práctica que "liberar la grúa con solo ~1h restante" — se agregó una regla de desempate que prioriza el traspaso más pequeño y descarta duplicados por grúa ya usada. Verificado en vivo en la página: el escenario N=4 ahora muestra exactamente una sugerencia — "Release Crane 1 at Sep 24, 05:50 PM ... let Crane 2 (already idle since Sep 24, 02:15 PM) finish its remaining 63 min of work — 3% less total labor cost". Números reales: costo original N=4 $71,439.88 → costo de dos segmentos $69,440.51 (Segmento 1: N=4, 10h50min, $50,293.43; Segmento 2: turno nuevo N=3 a las 19:00, 3h23min, $19,147.08) → ahorro $1,999.37 (2.8%), coincidiendo exactamente con lo que el usuario describió. Sin regresión: N=2/N=3 de ONE HOUSTON correctamente no muestran esta sugerencia (no hay el patrón de vecino inactivo antes de un break tardío); TANGIER MAERSK recargado desde cero (sin errores de consola, balance/spread en el mismo rango de pasadas anteriores, sin sugerencias de este tipo, como se esperaba dado que ese buque no tiene esa forma específica de desbalance). Alcance intencionalmente solo de costo (igual que la sugerencia de ET): no reescribe el Gantt, revenue, UGT ni ningún otro KPI — es una sugerencia superpuesta, no un cambio automático del escenario. Esto cierra la brecha documentada desde la octava pasada ("reducir cantidad de grúas a mitad de operación en un meal break") para el caso de un solo par de grúas adyacentes (una cadena de 3+ grúas retirándose en el mismo escenario no se intentó). **Target de MHPM corregido a 1.09, y filtro de "casi terminando" agregado a la sugerencia de ET (sep. 2026, misma sesión, pasada vigésimo cuarta):** el usuario corrigió el umbral verde/rojo de MHPM (era ≤0.92, debe ser ≤1.09 — la fórmula del KPI siempre fue correcta, solo el corte de color estaba mal) y pidió que la sugerencia de "trabajar durante el meal break" solo se evalúe cuando la grúa esté genuinamente casi terminando (~1-2 horas de extratiempo tipo ET+1, no varias horas más), dejando el análisis de costo (ET propio vs. turno nuevo con su garantía mínima de 4h) como el segundo paso, no el único criterio. Se agregó `ET_SUGGESTION_MAX_RESIDUAL_HOURS = 2` con un corte temprano en `findBreakAdvisories` (`if (residualMinutes > 120) return`) antes de calcular siquiera el costo del turno nuevo — cerraba un hueco real: en el caso de ONE HOUSTON N=4, la Grúa 3 mostraba una sugerencia de ET para un remanente de 3.39h (más barato en papel que un turno nuevo, pero no "casi terminando" bajo ningún criterio razonable). Verificado con datos reales: ONE HOUSTON N=3 pasó de 3 sugerencias engañosas (~5.1h de ET cada una, remanente real ~3.9h) a **cero** sugerencias; N=4 la sugerencia de la Grúa 3 desapareció correctamente mientras que Grúas 4/2/1 (remanentes 1.55h/1.26h/1.05h, todos ≤2h) se mantienen. La sugerencia de consolidación de grúas (pasada anterior) no se ve afectada, al ser una función independiente con su propio chequeo de costo. Sin regresión en TANGIER MAERSK (mismos costos exactos que la pasada anterior: N=2 $40,435.53, N=3 $38,220.59, N=4 $42,899.63) — confirma que el cambio solo tocó el filtro de la sugerencia y el umbral de color de MHPM, no el cálculo de costo/schedule subyacente. Notas de metodología (pantalla y PDF) actualizadas en ambos puntos. **Filtro de "casi terminando" corregido de nuevo — media midiendo el tramo equivocado (sep. 2026, misma sesión, pasada vigésimo quinta):** el usuario aclaró que "1-2 horas" se refiere a la ventana TOTAL de ET contada desde el inicio del propio meal break, no al trabajo real restante después de que termina: "la primera hora es la del mealbreak y la segunda hora sería la hora siguiente al mealbreak". Como `totalETMinutes` ya se define (desde la pasada decimoséptima) como el tramo continuo completo desde el INICIO del break hasta el término real de la grúa, el filtro debe topar ese `totalETMinutes` en ~2 horas (120 min) — no el remanente posterior al break por separado, que era la interpretación (incorrecta) de la pasada anterior. Dado que los meal breaks duran ~60-70 min por sí solos, el filtro anterior (remanente ≤120 min) en realidad permitía hasta ~190 min de ET TOTAL — más cerca de "ET+3" que del "ET+1/ET+2" que el usuario pidió desde el inicio. Se renombró la constante a `ET_SUGGESTION_MAX_TOTAL_ET_HOURS` (sigue en 2) y el filtro ahora topa `totalETMinutes` directamente. **Verificado con datos reales — es un recorte notablemente más estricto, y se le señaló así al usuario en vez de reportarlo como un ajuste menor:** con ONE HOUSTON, TODAS las sugerencias de ET que se mostraban antes tenían un `totalETMinutes` entre 121 y 274 min — todas quedan ahora fuera de la ventana estricta de 120 min, así que ONE HOUSTON actualmente no muestra **ninguna** sugerencia de ET con sus números reales (el caso más cercano: Grúa 1 en N=4 con 133.2 min, apenas 13.2 min por encima del límite). La sugerencia de consolidación de grúas (pasada 23) no se ve afectada, al ser independiente. Se confirmó que la función no quedó "muerta" en general: TANGIER MAERSK recargado desde cero sí muestra 2 sugerencias reales en N=4 (96.0 y 84.9 min, dentro de la ventana). Costos idénticos a pasadas anteriores en ambos buques (sin regresión al cálculo subyacente). Notas de metodología (pantalla y PDF) reescritas para describir el filtro correctamente. **Tarifa de ET recalibrada con un archivo de referencia real — estaba considerablemente subestimada (sep. 2026, misma sesión, pasada vigésimo sexta):** el usuario compartió `Conveniencia Mealbreak.xlsx`, un workbook construido específicamente para calibrar esta sugerencia, junto con una captura real del escenario de 4 grúas de ONE HOUSTON (26 sep. 2026, fin de semana, turno WS2). El archivo tiene una tabla "OPERATIONAL COST PER CRANE" con la cuadrilla completa de soporte de una grúa (32 posiciones: clerks, UTR drivers, lashers, técnicos, etc., no solo el operador) y una fila ET+1 a ET+3 (en pasos de 0.5h) perfectamente lineal: $3,739.08 / $5,608.62 / $7,478.16 / $9,347.70 / $11,217.24 — tarifa plana de $3,739.08/hora, sin discontinuidades por tramo. Se cruzó la fila "COST PER SHIFT" de esa misma tabla contra el `cost[0]` de los arreglos `SHIFT_WEEKDAY`/`SHIFT_WEEKEND` ya usados por la herramienta — coincide exactamente en los 17 códigos de turno de semana, confirmando que el archivo viene de la misma fuente maestra y que `cost[0]` sí representa la columna de 1 grúa (no hay bug de desfase de índice en el modelo de costo principal; el "Crane OP: 2" de la tabla refleja un estándar de rotación de 2 operadores por grúa, no que la tabla completa sea "de 2 grúas"). Se reemplazó `ET_PER_HOUR` (arreglo, solo se usaba el índice 0, $1,852.08/hora — sacado en una pasada muy anterior de un workbook distinto y aparentemente incompleto) por una constante única `ET_HOURLY_RATE = 3739.08` — aproximadamente el doble de la tarifa anterior, ahora representando la cuadrilla completa de soporte de esa grúa trabajando tiempo extra, no solo una tarifa aislada del operador. Verificado con datos reales: ONE HOUSTON recargado con los parámetros exactos de la captura del usuario (26 sep., fin de semana, Sur, costado Puerto) reproduce el costo N=4 exacto ($87,158.60) y el 7.63%≈8% de la sugerencia de consolidación de grúas (esa función no usa la tarifa de ET, por eso no cambia); las sugerencias de ET de ese escenario siguen en cero, ya que el filtro de 120 minutos (pasada 25) ya las excluye antes de llegar al costo. TANGIER MAERSK recargado desde cero: mismos costos exactos que pasadas anteriores (sin regresión), y sus 2 grúas con ET real (96.0 y 84.9 min) siguen mostrando la sugerencia, ahora con un costo de ET más alto y honesto ($5,982.53 y $5,290.80, antes ~$2,963/$2,622) pero todavía claramente favorable frente a un turno nuevo ($22,655.76) — el ahorro bajó de ~87% a un todavía sólido ~74-77%. **Demo standalone por email, sin datos reales (sep. 2026, misma sesión, pasada vigésimo séptima):** a pedido del usuario, se creó `Quay-Commander-DEMO.html` — una copia de este archivo con datos sintéticos (buque ficticio "MV Example Voyager", sin ningún dato real de ONE HOUSTON) precargados vía JS al abrir la página, sin necesidad de subir archivos. Un bloqueo del clasificador de permisos impidió leer los archivos reales de ONE HOUSTON para ese fin (correctamente, ya que embeber carga real en un archivo pensado para reenviarse por correo viola el principio de "los datos nunca salen del navegador"); el usuario eligió explícitamente datos sintéticos en su lugar. Este archivo demo **no vive en este repo** (vive en la carpeta Downloads del usuario y se entregó directamente) y no debe agregarse a `tools/` ni al arreglo `TOOLS` de `index.html` — es un artefacto de venta/demostración, no una herramienta del hub. **Atribución agregada al pie de página (sep. 2026, misma sesión, pasada vigésimo octava):** se agregó "Developed by the APMT Mobile Strategy Team" al `footer.qc-footer` de la herramienta real (no al demo de la pasada anterior, que es un artefacto aparte fuera del repo). Ver memoria `quay-commander-project-summary` (pasadas octava a vigésimo octava) para el detalle completo, incluyendo qué queda fuera (excepción de altura). |
| **Breakbulk Quote Desk** (BBK Calculator) | *No vive en este repo* — es un **Claude Artifact** (`https://claude.ai/artifact/VDFa7QSgiMKAfVuZui6eMd`), enlazado desde `TOOLS` en `index.html` con `external:true`. Calculadora de cotizaciones de carga breakbulk (BBK): el usuario registra una cotización (peso, dimensiones, ruta), la herramienta la valida contra una tabla de tarifas/referencias guardada (marcando ⚠ si hay datos de origen que se contradicen) y calcula factibilidad + precio; guarda todas las cotizaciones en el navegador (pestañas Quotes / New Quote / Reference & Rates) con export/import JSON para compartir entre equipos. Usa solo la capacidad `downloads` del runtime de Artifacts (no usa `db`) — por eso, a diferencia de SQDC, **no tiene la limitación de organization-internal**; cualquiera con el link puede usarla igual (el guardado es local a cada navegador, vía `localStorage`, no compartido). El artifact ya sigue el sistema de temas de la plataforma (`prefers-color-scheme` + `data-theme`) automáticamente, sin necesidad de un toggle propio. **Se le agregó (sep. 2026) un botón "← SMART Hub"** (`<a class="hub-back" href="https://al3xpinz0n.github.io/smarthub/">`) en la topbar, editando y republicando el artifact directamente (Version 2) — verificado visualmente que el botón aparece y el resto de la app sigue funcionando. Actualizado de nuevo (sep. 2026, misma migración de cuenta) para apuntar a `https://strategy-team-al.github.io/smarthub/` (Version 3). |

Yard, Gate, Rail y VAS siguen vacías — cada una debe mostrar su estado vacío hasta que se agregue su primera herramienta real.

### Control de acceso por área

Cada área requiere contraseña para entrar:

- **Strategy** usa una contraseña propia y distinta al resto: **`Mobile.Strategy`**.
- Todas las demás áreas usan el patrón **`ABS.` + nombre del área** (p. ej. `ABS.Vessel`, `ABS.Gate`, `ABS.Yard`, `ABS.Rail`, `ABS.VAS`).

En `index.html`, `AREA_HASHES` guarda el **SHA-256** de cada contraseña (nunca el texto plano) y el gate la valida con `crypto.subtle.digest` antes de desbloquear esa área en `sessionStorage` para esa pestaña del navegador.

⚠️ **Esto es un control de acceso liviano, no seguridad real.** El sitio es 100% estático y el repo es público — cualquiera con el link puede leer el HTML/JS fuente (aunque no el texto plano de la contraseña, sí el esquema `ABS.<Área>` / `Mobile.Strategy`, fácil de adivinar). Sirve para mantener a visitantes casuales fuera de áreas que no les corresponden, **no** para proteger datos sensibles. Por eso la regla de la siguiente sección sigue siendo innegociable: nunca poner datos operativos reales detrás de este gate — solo herramientas que procesan archivos que el propio usuario carga en su navegador.

## Arquitectura técnica y seguridad (no negociable)

- **100% client-side, cero backend.** No hay servidores, bases de datos ni APIs recibiendo datos de la terminal. **Única excepción documentada:** `Yard Strategy` usa Firebase Firestore para compartir el estado del patio entre todos los que abren la app — decisión explícita del usuario (ver tabla de herramientas de Strategy arriba). No repetir este patrón en otra herramienta sin que el usuario lo pida de nuevo.
- Los archivos que carga el usuario en cada herramienta (`.xlsx`, `.csv`, `.txt`) se procesan **exclusivamente en memoria del navegador**. Los datos operativos de la terminal nunca deben viajar por internet ni guardarse en GitHub. (De nuevo, `Yard Strategy` es la excepción: la *configuración* de estrategia del patio sí viaja a Firestore — no datos de carga/buques reales.)
- **Nunca** subir al repo: API keys, contraseñas en texto plano, credenciales, tokens, ni archivos de datos reales/hardcodeados.
- Como el repo es público, cualquier cosa que se suba es visible para todo el mundo — extremar cuidado con lo anterior.

## Librerías JS sugeridas (para las herramientas nuevas)

| Librería | Función | Caso de uso |
|---|---|---|
| **SheetJS** (`xlsx.full.min.js`) | Leer/parsear Excel/CSV | Reportes N4/XPS, listas de descarga, planes de patio |
| **jsPDF** / **html2pdf.js** | Generar PDF | Entregables de turnos y handovers |
| **Chart.js** | Gráficas | Tendencias de CMPH, tiempos muertos, productividad |
| **JSZip** | Comprimir archivos | Descarga masiva de reportes en un `.zip` |

## Estructura de directorios objetivo

```text
smarthub/                            <-- repo GitHub (carpeta local sigue llamándose Company-Projects)
├── index.html                       <-- Hub principal (SMART Hub, ya existe)
├── README.md
├── CLAUDE.md                        <-- este archivo
├── assets/
│   └── fonts/                       <-- Maersk Text (.ttf) usadas por index.html
└── tools/
    ├── strategy/
    │   ├── yard-strategy/index.html
    │   └── export-empty-analysis/index.html
    ├── vessel/
    │   ├── vessel-move-reconciliation/index.html
    │   └── baplie-correction/index.html
    ├── yard/<nombre>/index.html
    ├── gate/<nombre>/index.html
    ├── rail/<nombre>/index.html
    └── vas/<nombre>/index.html
```

Cada herramienta nueva va en `tools/<área>/<nombre>/index.html`, autocontenida (HTML+CSS+JS en un solo archivo, siguiendo el mismo patrón que `index.html`), con **todo su texto en inglés** (ver "Idioma: inglés siempre") y debe mantener consistencia visual con el hub principal (revisar `index.html` y el skill `apm-terminals-brand` antes de diseñar cualquier UI nueva). Al terminarla, agregar su entrada al arreglo `TOOLS` de `index.html` con el `area` correcto y el `href` apuntando a esa carpeta.

## Cómo trabajar en este repo

- Cada herramienta es un archivo HTML autocontenido (sin build step, sin frameworks pesados) — coherente con el enfoque 100% estático del hub.
- Antes de crear una herramienta nueva, revisar `index.html` para mantener el mismo lenguaje visual (tarjetas, áreas operativas, paleta de colores APM, tipografía Maersk Text) y el mismo idioma (inglés).
- El usuario revisa y publica los cambios con GitHub Desktop (Commit to main → Push origin) — no asumir acceso a git ni terminal salvo que el propio Claude Code lo tenga disponible en esta máquina.
- Existen otros dos documentos de referencia en la raíz del repo: `GUIA-PORTAL-HARDCODEADO.md` y `SETUP-VSCODE-GITHUB-CLAUDE.md` (este último es un runbook de instalación de entorno más amplio, orientado a un stack PHP/Laravel que **no aplica** a este proyecto — ignorar esa parte salvo que el usuario indique lo contrario).

## Próximo paso acordado

Seguir construyendo herramientas reales por área (Yard, Gate, Rail, VAS todavía no tienen ninguna) y agregar su entrada a `TOOLS` en `index.html` con el `area` correcto al terminarlas. Cuando una herramienta ya exista como archivo HTML fuera del repo (como pasó con las cuatro ya incorporadas), copiarla a `tools/<área>/<nombre>/index.html` tal cual si ya sigue la marca APM (Maersk Text, paleta oficial, inglés) — no reescribirla sin necesidad. Si no sigue la marca al 100% (caso de `vessel-move-reconciliation`, ver arriba), incorporarla igual tal cual salvo que el usuario pida explícitamente alinearla.
